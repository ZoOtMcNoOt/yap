# Auditor source-cited review-findings verification

**Status:** Exact executable candidate
`08b06f6db45e233d92035788dabdee7157a83d2f` privately qualified the
internal Auditor core. Hosted review, merge, HTTP/native/renderer/UI exposure,
production operation, and deployment remain pending.

## Exact qualified candidate

- Outcome: `auditor-source-cited-review-findings-qualified`.
- Public-safe evidence SHA-256:
  `2c1dbc0552233c377f0eff35bcfda3a5d761dbb548d06e638426cd232bddc376`.
- Acceptance-plan SHA-256:
  `0f8d01369da66b7d5d177e718ce736e950d690c08fe1d97e8c67dfdf28de1ee2`.
- Frozen synthetic corpus SHA-256:
  `30860909f44232f3227f63e77491efc0e55b6ea91048690f4dc23e55b57dca5a`.
- Eight cases across eight authenticated owners ran in three synchronized
  repeat waves. All 24 normal service calls and all three waves matched
  exactly. All 29 unique invocations reached the frozen terminal result with
  zero mismatch.
- The exact results contained 12 server-derived reports, 15 potential-
  contradiction findings, and 30 server-owned citations. Every report was
  noncanonical and review-required. The model selected only bounded evidence-
  index pairs; it could not author finding text, source identity, citations,
  or a free-form report.
- Exact terminal arithmetic was 12 complete, 13 evidence-unavailable, one
  failed invalid-output control, and three cancelled controls. Hidden-only and
  absent evidence were indistinguishable, and unavailable/cancelled/failed
  results contained no report.
- One production Auditor ticket was created for every invocation. Exactly 28
  tickets were submitted, 26 completed, one client cancellation and one
  deadline expiry were acknowledged to their exact terminal outcomes, and the
  sole pre-cancelled ticket was never submitted. Every submitted ticket was
  terminal, the one-lease-per-invocation contract held, and no nested agent-
  service lease was used.
- The gate independently compiled two current-authority knowledge generations,
  read back exact Auditor result and knowledge-tool audit cardinality across
  two owned PostgreSQL restart boundaries, observed no proposal writes, and
  passed every six-part database teardown assertion.

## Idle-only and warm complex-route boundary

The qualified complex profile has SHA-256
`4c5e5da836355e57ec43c6f1270eb9eb5839c6fd91e6dbf73389e37ce4cdf6a8`.
It retains the full Gemma limits: eight maximum sequences, 8,192 maximum
batched tokens, 8,192 maximum model length, `0.70` GPU-memory utilization,
7,680 maximum input tokens, and a 512-token output cap. Batch invariance was
enabled, prefix caching was disabled, request seed `0` was fixed, and the
provider and admission-broker identities remained unchanged through the
workload. The exact broker binary SHA-256 was
`0e71b9253eb3ebf29e83f6b222a67ba0f38caea2d36666fb6b408769d87e9d03`.

The live idle-only probe observed both active non-idle work and queued non-idle
work blocking Auditor admission, then observed Auditor admission resume after
the non-idle work reached a terminal state. The independent complex-capacity
probe held eight distinct owners, observed a ninth owner queued, and contained
every probe ticket. Both probes preserved provider and broker identity.

The three exact synchronized repeats prove repeatability inside one unchanged
already-warm provider process. They do not prove simultaneous inference,
cross-start or global determinism, sustained throughput/fairness, or a
production latency SLO. The idle-only proof establishes defer-while-busy
admission behavior; it is not a scheduler-utilization or production-load claim.

## Public verification

At exact clean head `08b06f6d...`:

- the portable server suite ran 1,501 tests: 1,454 passed and 47 declared
  platform/real-PostgreSQL skips;
- the governed fixed set ran 173 tests: 169 passed and four declared skips;
- the focused Auditor qualification/domain/service/audit matrix ran 52 tests:
  50 passed and two declared real-PostgreSQL skips;
- whole-server Ruff check, Auditor changed-file format check, `git diff
  --check`, Cargo format, locked all-target/all-feature Clippy with warnings
  denied, Cargo tests, and clean admission of all 333 protected candidate
  inputs passed.

The owner-private receipt and public stdout were mode `0600`. Their canonical
public evidence matched exactly. The exact checkout remained clean, and the
broker socket, provider containers, qualification networks, owned database
state, and owned processes were absent after independent read-back. Private
tenant/run identities, focus text, prompts, model outputs, evidence/citation
content, database rows, paths, DSN, and individual latency measurements remain
outside Git.

## Deliberate limits

Auditor is a qualified internal candidate, not a merged or product-exposed
workflow. The merged roster contains Scribe, Archivist, Student, Curator,
Librarian, Analyst, and Coordinator. Auditor accepts only an explicit
authenticated internal request, runs only under the idle-only admission class,
and returns source-cited potential-contradiction findings for review. It writes
no proposal, source, canonical knowledge, task, or action and performs no
autonomous mutation.

No evidence here proves simultaneous Qwen/Gemma residency, sustained multi-
user capacity/fairness, production p50/p95/p99 or availability, external
endpoints, scheduled autonomous execution, enterprise networking, deployment,
or approval to act on a finding. One Spark can run the unchanged full Qwen and
Gemma profiles only sequentially; a second owned GPU node and IT-controlled
private route remain required for simultaneous warm two-route promotion.
