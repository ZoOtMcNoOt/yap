# Audio Preprocessing and Language Routing Implementation Plan

**Status:** Active on `feat/phase6-preprocessing-pipeline`. Decision research
and public aggregate benchmark evidence are complete. The bounded verified ASR
catalog, frozen language decision, local primary-language conditioning, durable-
stage, normalization, imported-file advisory VAD, local language spans, and
isolated verify-only AmberNet batch-preflight slices now execute alongside pinned server
Nemotron fixed/automatic Transformers reference routes and a Cohere vLLM
adapter/image/launcher contract with focused Python 3.12, OpenAPI, Rust, and
TypeScript coverage. The pinned resident Nemotron NeMo adapter, cache-aware
scheduler, authenticated loopback service, checked image, and launcher also now
execute under focused tests. Their model-neutral candidate-safety lifecycle
passed at exact GB10 head `a21964c19e56648e9fddcb5200de419e59a7687c`;
provider promotion remains Phase 8 work. The resource harness now distinguishes cgroup residency, entrypoint RSS/
allocation extent, CUDA allocation/reservation, task/thread counts, and memory
events; version 5 of the runtime plan freezes separate GB10 ceilings for the
vLLM and NeMo candidates before the checked-head run. The retired Triton
experiment remains negative evidence.
Source-exact focused GB10 image smokes exist for Cohere vLLM and Nemotron NeMo
at executable commit `fcccf21e785b116b92cd8e46150a36b9b5ee91db`.
The isolated SpeechBrain component also has a historical source-exact receipt at
`04266c4bbffd0fd31eaf2afd0bcce42e0248344f`, but ADR 0026 supersedes that
runtime. The replacement AmberNet path has focused Windows real-model and
cross-architecture frontend/logit parity evidence. Exact executable commit
`c6862262fa36a83bcd40a7bffa65ec6429ec097e` also passed a focused ARM64 build,
real-artifact `ContainerLidWorker` execution, locked-resource measurement, and
teardown smoke. Exact executable head
`a21964c19e56648e9fddcb5200de419e59a7687c` then passed the final source-exact
ARM64 production-worker repetition. Exact executable head
`05d1b82017447df04d46ccc8fa729c5a6a0d0b13` subsequently passed the focused
assembled Windows-desktop/private-server route through durable preprocessing,
one tunnel interruption and recovery, server-authoritative result publication,
verified History review, and complete remote teardown. That closes the focused
advertised-route integration smoke without promoting a provider or completing
the Phase 6 gate. Exact executable head
`74322bf42c22058ffa88620f28cd4bf118ad8c01` subsequently passed the focused
current-host automated no-server Preview lifecycle: 12/12 paced native cycles
and all nine short-boundary cases completed without drops, and the unattended
release-mode UI run proved cancellation recovery, local fallback, save/delete,
production quit, and complete recording/model/process/listener teardown. The
accessibility rerun and complete frozen Phase 6 matrix remain open.
Fail-closed English
Cohere attention alignment also executes and has current-source contained GB10
lifecycle evidence without being advertised. The tiny SpeechFlow LID13 challenger
has been rejected before qualification, FireRedLID failed client payload
preflight, Whisper base failed broad global-top behavior, and the official base
and tiny Q5_1 releases failed the 500-ms-hop client throughput preflight. A
frozen disjoint `en-US`/`es-US` Whisper-tiny qualification then passed 13 gates but failed both
natural-switch coverage gates, so the narrow route is also rejected. The
exact AmberNet 1.12.0 static QDQ INT8 route also failed the original zero-false
natural/noisy transition threshold. The product owner subsequently accepted
that exact released artifact as a bounded, abstaining resident component rather
than continuing model research. The implementation requires three observations,
a `0.40` margin, explicit user-selected regional locales, and visible primary
fallback. A distinct 58-clip holdout was consumed once after threshold freeze
and produced 54 correct alternates, one abstention, three wrong alternates, and
zero false alternates when the primary was correct. The artifact is verified
local-import only while NGC redistribution review remains open. A focused
release-mode i7-13850HX proxy restricted the process to eight logical CPUs and
proved exact-source routing and teardown. Four configured Nemotron inference
threads were oversubscribed; the executable two-thread default completed the
combined path at `0.431` RTF and about 1.61 logical-core equivalents over
continuous source time. A one-thread diagnostic remained below real time at
`0.484` RTF but loaded materially more slowly. A source-paced follow-on accepted
all 3,840 ten-millisecond frames with no loss through the bounded 64-frame local-
ASR queue, reached a 42-frame high-water mark, drained 864 ms after the 38.4-
second source ended, averaged 1.765 logical cores (22.066% of the eight-CPU
budget), and measured 0.752 ms p95/6.966 ms maximum scheduler wake delay. The
language path added about 58.64 MB of private memory and left about 7.92 MB
after teardown. A four-logical-CPU repeat also lost no frames, reached 45/64
queued frames, drained in 911 ms, averaged 1.773 logical cores during paced
input, and measured 8.023 ms p95/45.864 ms maximum scheduler wake delay. Its
unpaced combined pass used 3.71 of four cores and is saturation evidence, not an
interactive-use claim. These proxies are not rendered-UI, sustained release-
lifecycle, minimum-device, battery, or thermal qualification. The separately frozen representative natural-switch
quality target completed and failed; that accepted limitation fixes the route
as an explicit, default-off Preview rather than remaining an unfinished pass
claim. Current-host resource/interference and sustained lifecycle passed the
focused target-client gate at
`74322bf42c22058ffa88620f28cd4bf118ad8c01`; the complete Phase 6 gate is not
complete. Representative
low-end physical battery/thermal certification is deferred until default-on or
Phase 10 release qualification.

The completed enabled-language development search tested 26,460 bounded
SpeechBrain policies and found no candidate meeting the full safety, coverage,
and latency thresholds. The distinct first-party TalTech VoxLingua107 EPACA
release then failed its bounded development-only behavior preflight: 73/84 case
pluralities and 1,098/1,539 two-second windows did not beat the rejected official
SpeechBrain baseline of 77/84 and 1,101/1,539. The stop rule prevented an enabled-
language policy sweep, export, packaging, or third holdout.

A bounded follow-on released-checkpoint screen before the AmberNet product
tradeoff found no alternative resident candidate. Two apparent VoxLingua107 alternatives duplicate already
rejected weights; the small Whisper and Vakgyata releases are narrow regional
classifiers; Simba-SLID-49 is regional and approximately 378 MB; and TalTech
XLS-R plus ESPnet GeoLID/MMS releases are approximately 1.26-3.9 GB. This closes
obviously ineligible acquisition work without claiming the future candidate
space is empty. The released `parakeet-rs` 0.3.6 Nemotron adapter also fails
API/provenance preflight: it strips the model's language-tag tokens from every
public transcript surface, so using it would require an unreleased fork or
replacement decoder plus a separate community ONNX export.

**Base:** executable-ownership review merge
`a80934d844a068110e7f86b30b6e29d35146db57` from
[PR #59](https://github.com/mcnatg1/yap/pull/59).

**Canonical owners:** Rust owns user language preferences, imported-job
decisions, source preparation, the local acoustic-LID/span policy, local
inference lifecycle, and native result admission. The server owns the versioned
provider capability catalog, heavy preprocessing/inference stages, independent
language verification, and server-authoritative result production. React
renders native/server state; it does not become a third settings, routing, or
pipeline authority.

**Scope:** Complete canonical Phase 6: deterministic normalization and advisory
VAD, source-bounded chunk/decision manifests, primary-language and per-job UX,
one bounded resident local acoustic-LID component, offline automatic language
switching, within-utterance source-time language spans, guarded long-recording
LID, fixed and explicit dynamic ASR routing, truthful word-timing evidence,
durable retryable pipeline stages, a performance-gated Cohere vLLM runtime, and
an independent Nemotron NeMo streaming gate. Preserve the merged Phase 5 path
and keep later identity,
speaker diarization, knowledge, secure-edge, and enterprise deployment work in
their accepted phases.

**Naming rule:** Executable modules, functions, tests, runtime configuration,
containers, and contract revisions describe their owned behavior, not the
roadmap phase in which they were introduced. Phase-number names are reserved
for this plan, actual one-time phase gates/evidence, and frozen legacy wire or
persistence tokens that must remain backward-readable.

The living [decision and evidence queue](2026-07-17-voiceos-decision-evidence-queue.md)
decomposes the unchecked work below and records discussed future/open questions.
It is a routing aid; this plan and ADR 0024 remain the Phase 6 authority.

## Architecture authority

### Applied decisions

- [ADR 0003](../../adr/0003-long-term-voice-architecture.md): user confirmation,
  multiple probes, and no silent LID routing remain required.
- [ADR 0004](../../adr/0004-background-diarization-okf-agents.md): critical-path
  isolation, bounded background work, recoverable failures, and aligning raw
  transcripts remain required. Its diarization algorithm details do not apply.
- [ADR 0006](../../adr/0006-silero-agents-state-machine.md): Silero VAD is an
  advisory boundary signal; VAD failure or false negatives never delete source
  audio.
- [ADR 0007](../../adr/0007-forced-alignment-engine.md): align raw ASR output,
  not polished text. The historical exact engine choice is amended by ADR 0024.
- [ADR 0008](../../adr/0008-speechbrain-lid-gate.md): LID is an assistive,
  user-gated batch decision. ADR 0026 supersedes its SpeechBrain model/runtime,
  download, two-window, and fixed-threshold details.
- [ADR 0014](../../adr/0014-server-tier-compute-topology.md): the client stays
  thin and the private server owns heavy model pools and official inference.
- [ADR 0018](../../adr/0018-three-repo-topology.md): Phase 6 remains in the
  staged monorepo; repository extraction waits for Phase 10.
- [ADR 0019](../../adr/0019-local-streaming-model-selection.md): the desktop
  retains one pinned local Nemotron ASR and no local ASR router. ADR 0024's
  narrow amendment permits one bounded auxiliary acoustic-LID component without
  creating another lifecycle or durable-state owner.
- [ADR 0020](../../adr/0020-meeting-capture-diarization-authority.md): retained
  source audio is authoritative, client VAD is advisory, and result revisions
  may truthfully omit alignment.
- [ADR 0023](../../adr/0023-bounded-live-priority.md): new server work preserves
  the bounded live-preference and per-owner scheduling contract. The executing
  Phase 5/6 development path still has one fixed `development-loopback` owner;
  it does not claim authenticated multi-tenant fairness.
- [ADR 0024](../../adr/0024-global-language-routing.md): the provider
  catalog, primary-language policy, local language-span engine, guarded batch
  preflight, Nemotron auto mode, alignment gates, and benchmark-backed
  exclusions are canonical for Phase 6.
- [ADR 0025](../../adr/0025-provider-specific-asr-serving.md): Cohere batch uses
  the gated vLLM candidate, Nemotron keeps the Transformers reference and gates
  NeMo streaming independently, Triton is retired, and SGLang stays outside ASR.
- [ADR 0026](../../adr/0026-ambernet-batch-language-preflight.md): the server
  verifies one imported AmberNet artifact, samples five strict start-to-tail
  regions, recomputes the decision at both client/server boundaries, and always
  requires user confirmation.

### Superseded or intentionally ignored details

- Do not revive ADR 0002's CrispASR runtime.
- Do not implement ADR 0004's diarizer/vault, ADR 0006's historical umbrella
  orchestrator, ADR 0007's unbenchmarked Canary/Wav2Vec2 choice, ADR 0008's
  `0.70` shortcut, ADR 0009's solo TCP knowledge worker, or superseded ADR 0015.
- Do not infer support from a tokenizer, prompt dictionary, or adaptation-ready
  locale. No Phase 6 model fine-tuning pipeline is authorized.

### Deferred decisions

- Phase 7: Entra/MSAL, token-derived ownership, authorization, revocation, and
  audit identity, including replacement of the fixed development owner.
- Phase 8: speaker inference, speaker reconciliation, contact/profile identity,
  and named attribution.
- Phase 9: Google OKF, permission-safe retrieval, agents, and MCP.
- Phase 10: persistent supervised model services, warm/multi-worker and mixed
  live/batch capacity promotion, production observability, DNS/certificates/
  ZPA/firewall policy, secure-edge promotion, enterprise deployment,
  publication governance, and repo split.
- Automatic cross-provider switching remains outside Phase 6. Client-local
  dynamic detection and within-utterance language diarization are now accepted
  Phase 6 outcomes and still require representative benchmark proof before they
  can be advertised.

## Executable baseline to preserve

- Phase 5 accepts only canonical mono PCM16/16 kHz WAV, creates an immutable
  Yap-owned spool, persists one native job ledger, and drains create/upload/
  commit/status/result/cancel over the numeric-loopback development contract.
- The Phase 5 isolated Cohere worker and its immutable NVIDIA 26.06/Python 3.12
  lock remain the only supported narrow fixed-language baseline. It is raw
  Transformers/BF16 inference in a transient one-job container. The pool has one
  running slot and two queued slots; it is not a warm or continuously batched
  model service or a universal accuracy tier.
- Local live fallback remains the in-process pinned `sherpa-onnx` Nemotron
  bundle. Its native stream accepts the confirmed BCP 47 primary-language
  option, which Yap applies on creation and reset. Automatic tags are filtered
  before the public result. The optional explicitly imported AmberNet 1.12.0
  QDQ INT8 and Silero bundle now emits source-time language evidence and drives
  bounded exact-once stream handoff behind the existing `LiveRuntime` owner. It
  is exposed only as a default-off Preview: the representative natural-switch
  target failed, while resource/lifecycle and complete checked-head release
  gates remain open.
- Result publication remains server-authoritative and native-verified before
  History can present completion.
- Private audio, transcripts, scan output, host paths, and raw benchmark output
  remain outside Git, PRs, hosted logs, and public artifacts.

## Decision evidence

The aggregate public-fixture spike is recorded in ADR 0024. It is design
evidence, not per-language certification:

The [dynamic language detection evaluation](../../research/2026-07-16-dynamic-language-detection-evaluation.md)
also separates clip-level LID, finalized-segment tags, and true language
diarization. Phase 6 now requires the third capability locally; no candidate is
promoted until its span boundaries, ASR handoff, transcript continuity, and
resource behavior pass executable evidence. Qwen3-ASR and VibeVoice remain
server challengers rather than substitutes for that local contract.

The [ASR evaluation corpus and runtime qualification](../../research/2026-07-17-asr-evaluation-corpus-and-runtime-matrix.md)
separates natural transcript-quality evidence from deterministic duration and
load evidence. The repository's one short English fixture and repeated long
WAV are smoke/runtime inputs, not comprehensive quality certification. Phase 6
keeps a bounded current-route `en-US` regression and does not promote either
resident provider candidate. The provenance-locked, transcript-private,
per-slice subset remains reusable work for whichever server route is later
selected for promotion; future approved private-domain evidence remains an
explicit handoff.
The machine-validated `server/asr-evaluation-plan.json` is the runtime matrix
authority. Public corpora are model-specific comparators unless their manifest
proves contractual exclusion or creation after the exact model freeze; an
official test split alone is not uncontaminated promotion evidence.
Schema 5 records the native `nemo-nemotron-finalized` path as executable and
adds the predeclared provider resource profiles because
the bounded resident worker/service now runs finalized utterances and recording
windows. That name does not claim the client-facing `/v1/live` or WSS transport,
which remains unimplemented. The committed product catalog still excludes the
unpromoted Nemotron provider; any provider-neutral end-to-end qualification uses
an explicit candidate capability lock outside Git.
Exposure covers the full base/adaptation lineage, and any public-derived
augmentation inherits the source decision. Unknown lineage remains a comparator.
The first executing source-specific intake freezes FLEURS `es_419` test at
revision `70bb2e84b976b7e960aa89f1c648e09c59f894dd` under the external private
cache. Seventeen focused Python 3.12 source/comparator/runtime tests plus an
all-908-case real inspection verify its archive, TSV, CC BY 4.0 legal-code
digest, safe membership, float32 WAV shape, batch contract, runtime identity,
private atomic results, and aggregate duration without emitting paths or
transcripts. Its evaluation locale is `es-419`; the Cohere route remains
provider code `es`. It is a comparator only and cannot be relabeled as
`es-US`/`es-ES` or used alone to promote a future fixed-batch locale. The
product-owner decision does not require a second Cohere locale for Phase 6
while the batch-provider replacement question remains open.

Source vetting identified European Parliament original-language plenary
speeches recorded on 7-9 July 2026 as the first feasible public contribution to
the sealed post-freeze Reality Set. Those dates follow both exact model
revision timestamps, Parliament permits commercial and non-commercial reuse
with source acknowledgement, and the source page supplies speaker, language,
and start/end metadata. This is not yet promotion evidence. One private
acquisition preflight showed that a published 79-second speech download carries
about 30 seconds of adjacent debate on either side; an exact source-time trim
isolated the target, while the revised written record still differed from
literal delivery. The intake must therefore exclude interpreted audio, bind
the original and trimmed identities plus event metadata, retain attribution,
and complete double review/adjudication before the registry can mark any case
`created_after_model_freeze`. Corti Med-Dictate is accepted only as a
restricted evaluation comparator for medical dictation, and MUSCAT remains on
hold because its current dataset repository does not state an audio/data
license. Raw audio, references, model output, and acquisition receipts remain
outside Git.

The promotion loader now implements the review boundary without claiming that
the reviews exist. Every `independentPromotion` case, whether post-freeze or
contractually excluded, requires one case-level, transcript-free receipt bound
to the exact candidate set, locale, timestamp, reference, rights, and known
defects. Its private registry separately authorizes the two listeners,
adjudicator, locale reviewer, and rights decision owner and hash-pins the blind
assignment, listener, adjudication, human exact-locale, rights, source-identity,
attribution, and preprocessing artifacts. Duplicate JSON keys, replacement or
oversize artifacts, silent listener overrides, and locale subtags supported only
by a base-language marker fail closed. Opened-handle containment and a portable
artifact-path grammar reject raced junction escapes and Windows alternate data
streams. Source URI/retrieval time, suite/condition labels, speaker/timing
metadata, and audio shape must also match the reviewed case. The real
production-case selection, human work, selection freshness/consumption record,
and out-of-band registry trust anchor remain open for a later provider
promotion.

The private GB10 comparator then ran the exact locked Cohere revision through a
20-case screen and all 908 cases under the Python 3.12.3/NVIDIA PyTorch 26.06,
Torch 2.13/CUDA 13.3/BF16 contract. The full 3 h 5 min 26.1 s source completed
in 61.455 measured seconds at 181.04 audio seconds per wall second with 3.5549%
normalized word error rate. The isolated container had no network, no Linux
capabilities, a read-only root/source/model/corpus, and only a private result
mount plus a disposable executable PyTorch compiler cache. Case text and the exact run
receipt remain outside Git. Because Cohere's FLEURS exposure is unknown, this is
descriptive comparator evidence—not locale promotion, Cohere vLLM parity, production
capacity, or representative spontaneous/noisy/meeting/long-form evidence.
Independent evidence additionally uses a dedicated loader with a private
registry, out-of-band registry digest, and hash-verified candidate-lock, freeze,
and exposure artifacts binding the complete candidate set, source item, and
exact audio/reference hashes. The manifest cannot self-attest those facts;
schema v2 admits only unique natural source audio to the independent gate and
requires controlled suite/condition labels plus hash-bound derivation lineage
for every transformed comparator.
Each reference also freezes the primary and punctuation scoring profiles and an
optional private critical-token-set SHA-256. The canonical scorer emits only
hashes and aggregate counts: no transcript or private policy vocabulary enters
Git, hosted logs, or public evidence.
The registry also binds the exact scorer lock and canonical per-case evaluation
policy digest. Promotion cases execute only through the private manifest
adapter, which verifies the manifest, model, reference, scorer lock, and policy;
derives duration from the hash-verified PCM WAV; and requires a separately
digest-pinned inference-result/runtime lock binding the case, audio, hypothesis,
candidate lock, and exact model revision. Public serialization omits case and
policy hashes. Normalized critical-token scoring detects retention and swaps;
the separate exact-surface metric detects case, acronym, punctuation, number,
and unit form drift. General medical number/unit semantic equivalence remains
an explicit unimplemented gate rather than a Phase 6 completion claim. The
private inference-result lock also records whether provider-native terminology
context was unused or supplied, binding only the policy/request hashes and
bounded counts. All currently locked Cohere and Nemotron systems declare no
such support; language and attention prompts are not mislabeled as terminology
context, and no provider-specific glossary store is added.

- Nemotron automatic LID: 78/84 correct over 28 out-of-box language families,
  mean 134 ms/probe, and 1.293 GiB peak GPU allocation in the measured shape.
- SpeechBrain VoxLingua107: 77/84 over the same 28 families plus 3/3 Greek;
  selected CPU path mean 179 ms, p95 234 ms, and about 760 MiB peak RSS.
- SpeechBrain probe-length evidence supports at least eight voiced seconds per
  bounded probe; silence/noise and related-language confusions require VAD,
  multiple windows, and manual confirmation.
- Cohere attention alignment is currently English-only evidence: held-out start
  MAE 74.71 ms, end MAE 68.53 ms, minimum exact-word transcript coverage
  93.33%, zero-based heads `(0, 5)` and `(1, 6)`, and reflected median width 3.
- Nemotron auto mode proved useful per-utterance server language tags but did not
  prove the required local within-utterance language spans.
- The already pinned released `sherpa-onnx` 1.13.4 API cannot reuse local
  Nemotron as the detector: its Nemotron result path still removes language-tag
  tokens and the public Rust result exposes no language field. A private fork is
  not an accepted substitute for an upstream released API.
- A native Whisper tiny LID smoke probe reused `sherpa-onnx` but added about
  108 MiB immediately and about 192 MiB working set after two public probes next
  to loaded Nemotron. It is not a negligible always-on client classifier.
- The production-window comparator then rejected that baseline for broad
  automatic switching: 56/84 correct cases and 767/1,539 correct two-second
  windows over 28 FLEURS language families. A private measured repeat recorded
  0.077 evaluated-window RTF, 154 ms mean, 186 ms p95, 193 ms p99, and a
  standalone test-process peak of about 242 MiB working set/281 MiB private
  bytes. Whole-package power did not separate from the noisy pre-run host
  baseline, and Windows exposed no live thermal-zone instance; neither is
  accepted as energy/thermal evidence.
- A single frozen disjoint qualification also rejected the narrow Whisper-tiny
  `en-US`/`es-US` route. It retained the configured primary without a false route
  on 60/60 monolingual cases, selected the opposite primary on 56/60 cases, and
  produced 54/60 correct constructed second-language transitions with zero
  wrong transitions or source-audio duplication/loss. It nevertheless emitted
  only three of five expected natural language-order segments and matched two
  natural boundaries, below frozen minimums of four and three. Thirteen gates
  passed and two failed. The qualification is final: do not retune against it or
  reuse its evidence for another candidate.
- A released Whisper-base INT8 behavior proxy improved global-top results to
  64/84 correct case pluralities and 886/1,539 correct two-second windows, but
  still failed related-language families systematically. Its two artifacts
  total 159,792,560 bytes and its conversion lineage is not promotion-grade.
- The official MIT Whisper-base Q5_1 checkpoint and `whisper.cpp` v1.9.1 full-
  probability API then failed client throughput preflight. A one-thread run
  completed 314 windows in 770.1 seconds: 2.453 seconds/window, 0.408 windows/
  second, and only 20.4% of the throughput required by the 500-ms hop. The
  incomplete sequential prefix is private, nonrepresentative, and not scored
  for accuracy; no product runtime was added.
- The materially distinct official Whisper-tiny Q5_1 full-probability route was
  also rejected at the same one-thread throughput preflight. Across 32 speech-
  qualified production-shaped windows it averaged 1,219 ms with 1,241 ms p95
  and delivered 0.820 windows/second, only 41.0% of the required hop rate. No
  accuracy, tuning, natural-switch, or holdout work was run, and no product
  runtime was added. Extra resident threads would alter the CPU/ASR-interference
  budget and are not treated as an implicit rescue.
- A native CrispASR ECAPA-LID-107 challenger was also rejected. It reached only
  33/84 on arbitrary first two-second windows and 74/84 whole clips after the
  standards-valid `no` to `nb` alias, versus 77/84 for the ECAPA ONNX
  comparator. Two-second inference averaged about 250 ms, the process peaked
  near 848 MiB private bytes, and the minimal target compiled roughly 56,304
  physical C/C++ lines with ggml. No CrispASR source or model is incorporated.
- An evaluation-only ECAPA ONNX conversion reached 77/84 case pluralities and
  1,101/1,539 correct two-second windows at 62 ms mean/76 ms p95 with four CPU
  threads. It remains unpromoted: the apparent exporter leaves dependencies and
  the official model revision unpinned, its root scripts have no explicit
  license coverage, and a fixed-window pure-Rust tract probe averaged 459 ms
  with 473 ms p95 and about 255 MiB peak working set. Neither external source,
  model, specialized graph, runtime, nor raw output is incorporated.
- The Apache-2.0 SpeechFlow LID13 release is only 5,428,476 bytes with 1,047,440
  parameters and averaged 22.021 ms per two-second window on one CPU thread, but
  it was rejected before qualification. Its best strict FLEURS development
  threshold retained 354 correct/11 wrong/625 abstentions, while natural
  English/Spanish evidence retained 8 correct/1 wrong/47 abstentions. Four-
  second grouped calibration still produced two wrong decisions on 11 exact
  natural windows; a zero-wrong threshold retained only 2/11.
- FireRedLID advertises compelling broad coverage, but its immutable published
  checkpoint payload is 3,550,103,418 bytes and no documented small native
  client artifact exists. The weights were not downloaded, and archive size is
  not treated as runtime-memory or parameter-count evidence.
- The user explicitly authorized evaluation acquisition of NVIDIA AmberNet
  `1.12.0`; that authorization is not product-redistribution approval. The
  official 116,049,920-byte checkpoint restored under an isolated Python 3.12/
  NeMo 2.7.3 overlay on the pinned NVIDIA PyTorch 26.06 image. An independently
  implemented exact Rust feature stage reproduced the reference, and a static
  29,613,392-byte QDQ INT8 classifier passed FP32 parity and development latency.
  Clean behavior was strong: 323/340 FLEURS development clips, including 276/280
  supported cases, while a conservative policy routed 265/280 with zero wrong
  routes and held all 60 controls. It nevertheless failed the required temporal
  boundary: zero of four transitions on one natural English/Spanish recording,
  only 7/12 constructed 15 dB SNR transitions, and no enabled-pair policy that
  preserved unrelated-language safety while meeting natural coverage on a second
  recording. Those results reject the original zero-false natural/noisy gate;
  they are not rewritten as a pass. The later product decision accepts the
  exact QDQ INT8 route with three observations, a `0.40` margin, and visible
  primary fallback. Thresholds were frozen on 29 calibration clips before a
  distinct 58-clip holdout was consumed once: 54 correct alternates, one
  abstention, three wrong alternates, and zero false alternates when the primary
  was correct. The exact native frontend/runtime and verified local-import
  lifecycle now enter Yap, but the model is neither bundled nor downloaded.
  NGC product packaging obligations remain open. The deprecated Silero language
  classifier also fails maintenance/license preflight.
- A disposable GB10 dependency probe on July 20 used the exact pinned NVIDIA
  PyTorch 26.06 base. The unmodified image reported Python 3.12.3, NVIDIA Torch
  `2.13.0a0+8145d630e8.nv26.06`, CUDA 13.3, and ONNX 1.21, but included neither
  NeMo nor ONNX Runtime. An isolated `nemo_toolkit` 2.7.3 overlay subsequently
  imported `EncDecSpeakerLabelModel` and exposed the immutable AmberNet 1.12.0
  URL under that Python/Torch combination. The later isolated evaluation restored
  and exported the model successfully; the full NeMo
  overlay selects Transformers 4.57.6 and protobuf 5.29.6, conflicting with the
  production worker's Transformers 5.13.1 and base `grpcio-tools` requirements;
  therefore it remained a separately locked disposable export image and was not
  layered into the Cohere/Nemotron worker.
- The model-independent classifier-output boundary is now executable in
  `language/acoustic_language_classification.rs`. It validates the locked label
  count, unique base-language codes, and enabled-locale mapping once when the
  component loads; the observation hot path validates finite full-vector logits,
  computes score and strongest-global-competitor margin over every label,
  abstains on a tie or a winning disabled language, and only then maps the
  winning base language to the user's selected regional locale. The routing
  policy now has one validated acoustic-evidence threshold value object and can
  hold separately for insufficient speech, absolute score, or global margin;
  observations reject scores or margins outside `[0, 1]` at admission rather
  than failing later while publishing evidence. Focused classification,
  diarization, pipeline, and exact-once router tests pass. This is reusable
  input-contract work. AmberNet has now been selected as the bounded Phase 6
  candidate without claiming that its original natural/noisy threshold passed.
- The official ECAPA graph also failed its smaller-format feasibility check.
  FP32 reached 989/990 enabled-pair clean development windows but only 49/58
  exact natural windows. Two approximately 22.8 MB QDQ INT8 variants collapsed
  to 365/990 and 399/990 clean decisions. Since Q8 already fails, no Q4 ECAPA
  derivative will be inferred or promoted without a distinct proven method.
- Qwen3-ASR-1.7B is a serious server batch/coverage challenger, but its current
  streaming path is vLLM-only, single-stream, unbatched, timestamp-free, and
  reprocesses accumulated audio. It requires exact GB10 evidence before route
  promotion.

Runtime selection remains model-specific under ADR 0025. Cohere batch uses the
digest-pinned NVIDIA vLLM 26.06/Python 3.12 candidate behind the checked
`BatchWorker` seam; the raw Transformers path remains its correctness and
rollback reference. Nemotron 3.5 is cache-aware FastConformer-RNNT, so its
current server correctness path stays on pinned Transformers and its model-
native streaming promotion candidate is a NeMo toolkit cache-aware service, not
vLLM or SGLang.
NVIDIA's Nemotron NIM remains ineligible until DGX Spark support and exact model
identity are unambiguous and independently verified.

Performance remains an explicit Phase 6 concern. Cohere's vLLM gate measures
exact output, lifecycle, cold/warm latency, throughput, p50/p95/p99 latency,
GPU/CPU memory, bounded admission, cancellation/recovery, teardown, and
c1/c2/c4/c8 behavior. Nemotron NeMo receives a separate correctness, streaming,
cache-state, concurrency, cancellation, lifecycle, and resource gate. Yap does
not assemble cross-owner tensors or infer one provider's promotion from the
other. Authenticated admission and persistent supervised mixed-load production
capacity remain later gates.

## Ordered implementation slices

### 1. Version the language, provider, and timing capability contract

- [x] Define one bounded catalog schema shared by OpenAPI, Python, Rust, and
      TypeScript projections.
- [x] Record provider/model revision, BCP 47 locale, execution mode, quality
      tier, language suggestion/tag support, alignment support, provenance/
      license identity, and promotion-evidence revision.
- [x] Advertise a catalog only when its locked runtime and required artifacts
      are verified. Health remains small; catalog retrieval is separately
      bounded and versioned.
- [x] Keep a last-known verified native snapshot for offline explanation
      without treating it as current server availability.
- [x] Reject duplicate locales/modes, unknown tiers, invalid BCP 47 tags,
      adaptation-ready Nemotron entries, mutable revisions, and unbounded text.

### 2. Add one Rust-owned primary-language and per-job decision flow

- [x] Persist a versioned, bounded primary BCP 47 locale under canonical app
      data with atomic/no-follow behavior and safe recovery.
- [x] Setup must require confirmation; the OS locale may suggest but may not
      silently save the decision.
- [x] Settings may edit the primary language and imported jobs freeze and
      display the exact primary/manual disposition.
- [x] Keep the catalog-derived recording-language selector visible and
      model-independent. It currently offers only the advertised, gated Cohere
      `en-US` route and must gain an alternate only from a later route-specific
      promotion; Phase 6 does not add a Cohere locale solely to populate the
      selector.
- [x] Recordings below the locked batch-preflight duration/speech bounds skip
      AmberNet and use the primary/manual language. A job override never
      rewrites the saved primary language.
- [x] Local warmup requires the confirmed primary locale, validates the exact
      32-locale out-of-box allowlist, applies it to stream creation/reset, fails
      unsupported locales visibly, and invalidates idle warm state before a
      preference change commits.
- [x] React remains a projection of native preferences and catalog state; no
      localStorage language authority or hard-coded permanent provider matrix.

Focused invariant closure now retains bounded picker/drop proof before staging a
new import durably as non-runnable `accepted`, projects active playback, and only
then admits the exact unlocked row to `preflighting`. Snapshot recovery consumes
retained proof and cannot reconstruct selection authority from a ledger path;
cancellation cannot re-register authority. Preflight creates one immutable
Yap-owned normalization/VAD artifact, uses the configured language for short
recordings, or dispatches the bounded AmberNet preflight for long recordings.
The exact decision is frozen only after durable user-confirmation evidence, then
the same capture is promoted to remote upload. Already-locked Phase 5 rows retain
their direct server-queue compatibility path. Restart cancellation reconciles a
persisted LID request against its original server before forgetting dispatch
identity, and expired derived data cannot be rebound to changed external bytes.
Catalog, upload, result-polling, retry, and failure handling preserve one exact
job ID. Any failed retry, deferral, terminal-error, or retention write opens an
in-memory drain circuit; schema 8's metadata-only SQLite write probe must commit
before preprocessing or another catalog/upload/result request can run. Server
reference routing, alignment, representative promotion evidence, and final
phase gates remain incomplete.

### 3. Produce deterministic source-authoritative preprocessing evidence

- [x] Define normalization and advisory VAD output as source-time intervals and
      immutable stage evidence; never create a speech-only authoritative source.
- [x] Pin and verify the selected Silero artifact/license before enabling it.
- [x] Bound input duration, window size, padding, memory, CPU concurrency,
      output count, and serialized manifest size.
- [x] Preserve exact gaps and source offsets. VAD error continues with the
      retained source and an explicit unavailable/error result.
- [x] Keep current one-MiB transport bounds independent from VAD so uninterrupted
      speech or detector failure cannot create an unbounded chunk.

The executing imported-file path admits canonical mono PCM16/16 kHz WAV,
records identity normalization without altering source time, and optionally runs
the exact pinned Silero ONNX artifact through the existing `sherpa-onnx` CPU
runtime. It emits at most 4,096 ordered source-sample/source-millisecond
intervals or a bounded explicit error while retaining the complete source and
the independent one-MiB upload chunks. Installation is explicit; missing or
corrupt VAD state never triggers an implicit network download.

### 4. Make preprocessing stages durable and retryable

- [x] Add explicit normalization, VAD, LID decision, ASR, alignment, and result
      publication stage records to the existing job authority rather than
      creating a second queue.
- [x] Record input fingerprints, immutable component revisions, attempts,
      terminal outcome, retryability, and source-bounded evidence.
- [x] Make restart/resume idempotent. Retry one failed stage without rewriting
      capture identity, a confirmed language choice, or a prior result revision.
- [x] Bound retention and delete only Yap-owned derived artifacts; never delete
      an admitted external source because a stage failed or was cancelled.

Desktop schema 9 owns bounded attempt history for normalization, VAD, LID
preflight, and user confirmation. Server schema 5 owns ASR, alignment, and
result-publication attempts. Both authorities retain fingerprints, component
revisions, retryability, evidence hashes, terminal outcomes, and bounded history;
legacy rows remain readable without claiming history they never recorded.

### 5. Add the isolated AmberNet batch preflight

- [x] Supersede the SpeechBrain/Torch component with a separate CPU-only Python
      3.12.13 component pinned to NumPy 2.4.6, ONNX Runtime 1.27.0, and the exact
      AmberNet 1.12.0 static INT8 QDQ graph/frontend/107-label contract.
- [x] Make model delivery verify-only: accept one explicitly imported regular
      artifact with the locked size/SHA-256, reject any extra file, and expose no
      bundle, mirror, download, synchronization, or fallback path while NGC
      redistribution authority remains unapproved.
- [x] Select exactly five six-second regions at deterministic 0/25/50/75/tail
      positions for recordings of at least 30 seconds. Require 51,200 VAD speech
      samples in every region and preserve exact source offsets and probe hashes.
- [x] Split each region into two independent three-second graph executions,
      average logits only within the region, and use stable log-softmax evidence.
      Never concatenate, pad, or batch across requests.
- [x] Require all five normalized language labels to agree, every margin to be
      strictly positive, and the result to map to one enabled fixed locale.
      Silence, missing regions, disagreement, invalid/unsupported labels,
      ambiguous locale mapping, timeout, cancellation, component failure, or a
      client/server decision mismatch require manual choice.
- [x] Keep the suggestion assistive: persist immutable component/source evidence,
      prefill the picker only, require user confirmation, and never rewrite the
      saved primary locale.
- [x] Bound the worker to a non-root, networkless, read-only, one-thread CPU
      container with one CPU, 512 MiB memory/swap, 64 PIDs, and bounded output,
      temporary storage, queueing, cancellation, cleanup, and teardown.
- [x] Build exact executable commit
      `c6862262fa36a83bcd40a7bffa65ec6429ec097e` on ARM64 GB10, execute the real
      imported artifact through `ContainerLidWorker`, measure cgroup peak
      memory/CPU and cold latency under the locked limits, and prove teardown
      without an owned container/listener.
- [x] Repeat that source-exact image/resource/teardown check at exact executable
      head `a21964c19e56648e9fddcb5200de419e59a7687c`: the production worker used
      one CPU, 512 MiB, 64 PIDs, no network, a read-only root, and one-thread
      pools; five contract observations completed in 788 ms wall/670,672 CPU
      microseconds with 111,902,720 bytes current/peak cgroup memory, six peak
      PIDs, zero memory events, and complete teardown. The private aggregate
      evidence SHA-256 is
      `37fb2cad6c83c5e4084d07af18e1c9645cb758a29867d443bea368bdaf42bad7`.
- [x] Classify representative fixed-language/long-tail suggestion quality as
      unpromoted rather than starting another Phase 6 evaluation campaign. The
      executing path remains assistive, user-confirmed, and manual on ambiguity
      or failure. Keep the complete one-time Phase 6 resource/matrix gate open
      until the branch is frozen.

The desktop and server independently compute the same five source regions and
decision. Focused Python and Rust tests cover the four-hour selector bound
without duration-sized work, exact tail placement, graph/frontend/label identity,
model-directory verification, request materialization, backpressure, cleanup
fencing, redacted HTTP errors, user confirmation, retention, retry, and
cancellation. A focused Windows real-model smoke loaded the exact graph under
one-thread CPU ONNX Runtime; a disposable ARM64 comparison reproduced the
frontend within the recorded tolerance and produced identical full logits. The
later source-exact ARM64 worker smoke for
`c6862262fa36a83bcd40a7bffa65ec6429ec097e` peaked at 111,591,424 cgroup bytes,
six PIDs, and 682,363 CPU microseconds under one CPU, 512 MiB, no network, and a
read-only root. Cold container wall time was 0.842 seconds; no throttling,
memory-limit event, OOM, listener, or retained owned container remained. The
private reports and model remain outside Git. These are focused implementation/
resource evidence, not final frozen-head or Phase 6 completion evidence; the
synthetic silence workload is not accuracy evidence.

The superseded SpeechBrain image receipt at
`04266c4bbffd0fd31eaf2afd0bcce42e0248344f` remains historical evidence that the
older containment path executed and tore down. It is not evidence for the
current AmberNet artifact, dependency set, five-region policy, or resource
limits.

### 6. Add local language spans and route fixed/dynamic ASR without hidden providers

- [x] Pin the Whisper-tiny INT8 ONNX native acoustic-LID comparator,
      `sherpa-onnx` inference revision, exact artifacts, license/provenance,
      locale mapping, explicit installer lifecycle, and packaging path.
- [x] Select and pin the accepted resident detector: the exact AmberNet 1.12.0
      static QDQ INT8 graph, NeMo-compatible native frontend, one-thread ORT
      session, immutable 107-label order/aliases, full-label abstention, three-
      observation/`0.40`-margin policy, and user-selected Nemotron locale
      intersection. This accepts its measured limitation; it does not relabel
      the earlier natural/noisy gate as passing. Whisper tiny is rejected for
      broad automatic switching by the 84-case/1,539-window comparator, and
      native CrispASR ECAPA is rejected on accuracy, latency, private-memory,
      and dependency-surface evidence. A deterministic official-checkpoint
      SpeechBrain ONNX export and native ONNX Runtime probe now exist, but the
      original one-policy independent gate failed on two monolingual false
      switches and 8/30 natural Mandarin initial-route coverage. A pair-
      restricted two-stage policy was frozen from that now-development corpus.
      Its second disjoint gate passed coverage, wrong-route, and sustained-safety
      thresholds but failed on four false Mandarin initial routes across 30
      natural English clips. A primary-biased latched search across both rejected
      corpora could reach zero false routes only at 84.6% FLEURS coverage and a
      four-second p95 decision. The global-top-label adapter is rejected;
      a 26,460-policy enabled-set development search also produced no candidate
      meeting every frozen threshold, and thin pair-matrix cells are not
      certification. The materially different TalTech EPACA release then failed
      its bounded public comparator at 73/84 cases and 1,098/1,539 windows, so no
      policy sweep, export, packaging, or third holdout followed. The distinct
      lightweight SpeechFlow LID13 challenger was rejected before qualification
      because its calibrated English/Spanish behavior did not generalize to
      natural switch evidence. FireRedLID failed client payload preflight;
      AmberNet passed exact native export/parity and clean-clip preflights but
      failed natural/noisy switching; and deprecated Silero fails maintenance/license
      preflight. The bounded follow-on release screen
      found only duplicate rejected checkpoints, narrow regional specialists,
      or approximately 378 MB to 3.9 GB artifacts that fail coverage or payload
      preflight. The released `parakeet-rs` Nemotron adapter also strips language
      tags from its public result and would require an unreleased fork plus a
      separate export. A private token-label diagnostic proved the current
      sherpa runtime can expose the pinned Nemotron export's hidden tag IDs
      without a fork, but three valid natural German-English-German recordings
      emitted no English tag during their labeled English spans in either final
      or live-partial results. The same-model route therefore fails behavior,
      not only API shape. The released Whisper-base global-top proxy reached only
      64/84 case pluralities and 886/1,539 windows; its official 59.7 MB Q5_1 release
      then achieved only 20.4% of required hop throughput and was stopped before
      accuracy scoring. The smaller official Whisper-tiny Q5_1 full-probability
      route likewise achieved only 41.0% of required one-thread hop throughput
      and was stopped before accuracy scoring. The frozen narrow `en-US`/`es-US`
      Whisper-tiny route also
      failed its single disjoint gate after missing required natural language-
      order segments and boundaries, despite zero monolingual false routes,
      wrong constructed transitions, or audio loss. Custom training is not an
      implicit fallback and no further model search is required for Phase 6.
      A focused release-mode i7-13850HX proxy restricted the process to eight
      logical CPUs and routed all 614,400 samples of a 38.4-second fixture
      exactly once. It exposed four-thread Nemotron oversubscription. The
      executable two-thread default completed Nemotron plus AmberNet/Silero at
      `0.431` RTF, with 61.844 CPU-seconds and about 1.61 logical-core
      equivalents over continuous source time; a one-thread diagnostic reached
      `0.484` RTF with lower CPU use but materially slower load. The source-paced
      follow-on accepted all 3,840 ten-millisecond frames without loss, reached
      42/64 queued frames, drained in 864 ms, averaged 1.765 logical cores, and
      measured 0.752 ms p95/6.966 ms maximum scheduler wake delay. A four-
      logical-CPU repeat also lost no frames, reached 45/64 queued frames,
      drained in 911 ms, averaged 1.773 cores during paced input, and measured
      8.023 ms p95/45.864 ms maximum scheduler wake delay. Its accelerated
      combined pass used 3.71 of four cores. Private aggregate JSON remains
       outside Git. Current-host rendered UI/audio interference, sustained-
       session, restart/cancellation, redistribution, and packaging evidence
       remain open. Representative low-end physical energy/thermal evidence is
       a later default-on and Phase 10 release boundary. The proportional MVP
      gate must record completion, loss, text presence, and release-to-final
      latency at 250 ms, 500 ms, 750 ms, one second, 1.12 seconds, two seconds,
      and the retained longer short cases. Sub-word 250/500/750-ms inputs may
      finish blank; one-second-through-30-second speech-bearing cases must
      produce text. Timestamped phoneme-clipping and raw-accuracy scoring remain
      in the Phase 8 selected-model quality gate rather than being fabricated
      from truncated audio without exact references.
      The first separately frozen clean German-English product-route set has now
      been consumed once. It preserved exact source coverage and the required
      primary fallback but detected 0/4 must-detect natural alternate spans and
      matched neither entry nor exit boundary. A post-failure diagnostic of the
      exact implemented INT8 detector counted 68 speech-qualified alternate-region
      windows, only five alternate top labels, and one alternate observation
      above the frozen `0.40` margin, with no such false observation outside the
      alternate regions. Because whole-clip FP32/INT8 parity was 322/340 versus
      323/340 and the earlier FP32 diagnostic also missed three spans, record
      this as a short-window/domain failure rather than a quantization regression.
      Do not retune against the consumed set or relabel its failed quality
      target. The product owner accepted that measured limitation behind an
      explicit, default-off Preview; passing this consumed quality target is no
      longer a Phase 6 release-safety condition, while removing Preview requires
      new independent evidence. The original aggregate remains in the private
      external evidence root. Its exact machine-emitted object and SHA-256 were
      reverified, its Windows ACL is owner-restricted, and the audit-only native
      test revalidated the recorded failed-quality/passed-safety boundary without
      reading audio or invoking either model. The private path and hash receipt
      remain outside Git and hosted output. The three native private-evidence
      collectors now share one no-clobber publisher that writes `0600` on Unix
      and a protected owner/SYSTEM/Administrators DACL on Windows before any
      evidence bytes, so later duration and resource gates do not depend on a
      permissive inherited directory ACL.
- [x] Enforce the local quantization floor: never promote a derivative below
      Q4. Choose Q4, Q8/INT8, or higher precision per exact model only when the
      representative accuracy, low-end physical client CPU latency, resident
      memory, battery, and lifecycle gate passes. Preserve the tested local Nemotron INT8 route;
      do not rewrite its decision while evaluating an auxiliary detector.
- [x] Persist an explicit automatic-routing set containing the confirmed primary
      language plus user-enabled alternates from the executable local catalog.
      The control is default-off and visibly labeled Preview. Labels outside
      that set abstain; do not ship an implicit all-languages mode, infer
      alternates from geography, or describe availability as locale-specific
      qualification. The UI also discloses that initial partial text can wait
      for a three-second detector window. A shorter stopped utterance flushes
      through the confirmed primary; leaving alternates off preserves the
      ordinary low-latency primary-language path.
- [x] Replace the shared threshold with function-specific
      `PrimaryBiasedInitialLanguageSelector` and
      `SustainedLanguageSwitchDetector` policies. Initial selection must be
      bounded and latched; later evidence belongs only to sustained switching.
      Do not implement the rejected one-window/`0.30` global-top-label policy.
- [x] Expose product setup/control surfaces through the functional
      `AcousticLanguageDetector` contract while keeping AmberNet-specific model,
      frontend, label, and runtime behavior inside its concrete adapter. Remove
      the superseded Whisper product lifecycle; retain Whisper names only in
      historical comparator evidence.
- [x] Keep `LiveRuntime` as the sole lifecycle owner. The LID module accepts
      bounded source-time speech audio and emits ordered `LanguageSpan` evidence;
      it never transcribes, owns capture state, or selects among ASR models.
- [x] Implement bounded windows/hops, speech masking, minimum evidence,
      hysteresis, lookahead, and ambiguity/failure fallback to the confirmed
      primary locale. Preserve scores as uncalibrated evidence.
- [x] On a confirmed switch, partition bounded held source audio at the accepted
      boundary, feed every sample exactly once, finalize the prior Nemotron
      stream, create the next stream with the supported locale, and append
      finalized segment text without rewriting it. Representative continuity
      promotion evidence remains part of the next unchecked item. A focused
      six-second English plus six-second Japanese pinned-model diagnostic found
      and closed the overlapping-window retention bug, accepted the constructed
      switch within 250 ms, and routed all 192,000 samples exactly once.
- [x] Finish the deterministic within-utterance Preview safety contract at the
      Rust session owner. Existing policy and routing tests cover stable and
      rapid/false switches, ambiguity, weak/no-speech evidence, unsupported
      labels, overlapping detector windows, exact-once handoff, and pending-state
      reset. Session-boundary tests now additionally prove sub-window correction
      flush, admitted-audio drain after detector failure, transactional primary
      fallback on holdback exhaustion, pending-state reset across cancelled or
      restarted sessions, and visible primary operation when local detector
      artifacts are unavailable.
      Focused verification passed 123 language-related tests; eight real-model or
      private collectors remained intentionally ignored.
- [x] Complete the remaining real-runtime Preview release boundary on the
      available Windows system: installed-artifact local operation with no
      configured or listening server, repeated sessions, restart/cancellation,
      rendered-UI and capture interference, and bounded resource teardown. The
      checked-head native repeated-session and release-mode physical-microphone/
      rendered-UI collectors are implemented and bind the actual processor and
      processor count. They fail closed on an optional named-hardware
      constraint, a listening numeric-loopback server, configured server state
      in the isolated profile, dirty/different source, in-repository evidence,
      missing resident artifacts, degraded routing, responsiveness limits, or
      retained private/model-snapshot content. They are composed by
      the [target-client qualification runbook](../../runbooks/target-client-language-routing-qualification.md).
      Exact executable head
      `74322bf42c22058ffa88620f28cd4bf118ad8c01` consumed both channels:
      12/12 paced native cycles and all nine prepared-audio cases completed
      without drops, while the unattended UI run proved four-cycle cancellation
      recovery, local fallback, save/delete, production quit, and complete
      recording/model/process/listener teardown. The launcher
      automates a 30-second default-microphone/UI lifecycle smoke; the checked-
      head prepared-audio profile owns speech/transcription evidence. A longer
      manual physical-device soak and calibrated low-end energy/thermal
      evidence cannot be inferred from this development host and are deferred
      to default-on or Phase 10 release qualification. They do not block this
      explicit Preview. The current-host safety item is complete; removing the
      Preview label still requires different independent quality evidence.
      The completed constructed, related-language, noisy, silence, and overlap
      results plus the consumed clean natural result remain their recorded
      evidence—including the natural product-route failure at 0/4 must-detect
      transitions. Exact source coverage and fallback passed. Do not rerun or
      relabel that completed quality result; it blocks removing Preview, not
      completion of the remaining safety gate.
- [x] Use the same versioned span contract for local and server workflows. A
      server may independently verify/reconcile evidence but may not silently
      rewrite client history or create a second client-state owner.

- [x] Retain Cohere for its enabled fixed-language baseline and add a pinned
      server Nemotron reference worker for enabled fixed broad/fast routes.
      Describe or select overlapping routes only from locale- and workload-
      specific evidence. Select between the exact DGX Spark NeMo toolkit service
      and Transformers candidates with locked-model correctness/resource evidence;
      do not substitute a generic vLLM/SGLang path or an unsupported NIM profile.
- [x] Add explicit Nemotron `target_lang=auto` dynamic mode. Preserve a valid
      emitted locale tag for every finalized segment.
- [x] Keep server terminal-tag changes bounded by finalized VAD/endpoint
      utterances. Do not mislabel that server contract as within-utterance
      diarization; the local `LanguageSpan` gate above owns that claim.
- [x] Mark missing, invalid, disabled, or adaptation-ready tags `Unknown` and
      require visible review; never silently relabel them as the primary locale.
- [x] Keep one provider for the entire automatic job. Do not silently dispatch
      individual utterances between Cohere and Nemotron.

The reference route uses the exact NVIDIA revision
`f3d333391852ba876df169dcc9ba902d25b6ab0b` through Transformers 5.13.1 in the
locked NVIDIA PyTorch 26.06/Python 3.12/BF16 environment. A complete-source,
hash-bound utterance plan partitions long recordings into ordered windows of at
most 30 seconds, uses advisory VAD only to choose boundaries, and falls back to
hard bounds without dropping audio. Fixed requests retain their exact BCP 47
locale as authority; valid model-emitted locale metadata is stripped even when
it names another known locale, while malformed/unknown tags still fail closed.
Explicit dynamic jobs retain `und` externally and `auto` only at the
provider boundary, preserve ordered detected/unknown language segments, and
remain on Nemotron for the whole job. The shipped catalog remains Cohere-only
until a promotion gate changes it.

The shared version-1 16 kHz `LanguageSpan` contract now distinguishes local
`clientDecision` boundaries from server `serverUtterance` boundaries. Dynamic
Nemotron results cover the complete canonical source timeline with one
model/utterance-plan-bound span per finalized utterance; every lossless text
segment carries `sourceSpanIndex`. Mixed or incomplete text evidence leaves the
whole server utterance `und` rather than inventing a finer source boundary.
The isolated worker, durable server result, OpenAPI schema, and native History
admission all fail closed on missing, unbound, discontinuous, or mismatched
evidence. Focused Python 3.12 and Rust tests pass; natural/constructed local
switch promotion and the frozen runtime parity rerun remain open.

### 7. Prove the provider-specific ASR runtimes

Phase 6 qualifies these replaceable candidates only far enough to prove the
bounded server contract, current advertised `en-US` contract compatibility,
deterministic duration/concurrency safety, cancellation/recovery, resources, and
teardown.
It does not promote either resident service or run a broad Cohere natural-
quality campaign. Phase 8 reuses bounded single-speaker/long-batch controls when
deciding whether Tiron is meeting-only, replaces Cohere batch work more broadly,
or is rejected.

- [x] Pin `nvcr.io/nvidia/vllm:26.06-py3` by immutable ARM64 digest and assert
      Python 3.12, Torch/CUDA, vLLM, Transformers, audio dependencies, exact
      Cohere model artifacts, and license/provenance identity.
- [x] Implement the bounded Cohere vLLM transcription adapter and checked
      launcher behind the existing worker contract. Require numeric loopback,
      a private API key, exact version/model readiness, bounded multipart I/O,
      response validation, cancellation acknowledgement, and containment
      fencing. Keep durable state, routing, admission, retry, and publication in
      Yap.
- [x] Retain the Transformers Cohere/Nemotron reference paths and remove the
      retired Triton backend, client, scheduler, parity, and benchmark machinery.
      Preserve the private Triton results as historical negative evidence.
- **Phase 8 provider-selection work:** If Cohere remains a serious promotion
      candidate, run its complete representative-quality and rollback
      comparison against Tiron. The Phase 6 candidate-safety lifecycle is not a
      substitute for that model decision.
- **Phase 8 provider-selection work:** In that comparison, measure Cohere
      cold/warm latency, queue time, p50/p95/p99, audio-seconds/second,
      GPU/CPU/private memory, model-output behavior, and representative quality.
      Continuous batching stays internal to vLLM; Yap never concatenates or pads
      one owner's audio into another request.
- [x] Implement the pinned Nemotron NeMo candidate behind the same provider-
      neutral contract: exact `.nemo`/runtime/config identity, one scheduler
      owner, bounded independent requests, authenticated numeric-loopback
      transport, job-specific cancellation, fail-closed result validation, and
      checked container/launcher boundaries. Do not send Nemotron through vLLM
      or inherit Cohere proof.
- **Phase 8 provider-selection work:** If Nemotron remains a serious promotion
      candidate, run its independent representative locale/quality,
      output-behavior, percentile, and rollback comparison before selecting it.
      Do not inherit Cohere or Phase 6 candidate-safety evidence.
- [x] Run the proportional Phase 6 duration plan: deterministic 250-ms through
      30-second local-live boundaries through the hash-bound `short-boundaries`
      profile, one automated 30-second default-microphone/rendered-UI lifecycle
      smoke,
      and the already-completed model-neutral server candidate-safety duration,
      admission, cancellation, resource, and teardown controls. Retain the
      `complete-local-duration-ladders` profile through two hours for default-on
      or Phase 10 release qualification together with a longer manual
      physical-device soak instead of spending that wall-clock time on a
      default-off Preview. Reuse bounded long-batch controls in Phase 8 when
      comparing Tiron with any surviving Cohere route.
- [x] Keep both resident provider runtimes unpromoted in Phase 6. Any later
      promotion requires route-specific frozen evidence, rollback proof, and
      review; persistent supervised multi-owner capacity remains Phase 10.
- [x] Run the composed model-neutral Phase 6 candidate-safety lifecycle. Exact
      GB10 head `a21964c19e56648e9fddcb5200de419e59a7687c` passed all 18
      Cohere vLLM/Nemotron NeMo duration, identity, language-contract,
      cancellation, admission, resource, and teardown children without promoting
      either provider.

At exact executable source SHA
`fcccf21e785b116b92cd8e46150a36b9b5ee91db`, focused GB10 smokes exercised both
full locked models through Yap's production adapters. The non-root Cohere image
`sha256:761d78efa390f84168827fb5f1075faa7720053efbc122fa74d76273d4a483bf`
ran `VllmTranscriptionClient`, produced a bounded hash-receipted result, and
cleanly removed its container and listener. The Nemotron image
`sha256:174003c3ae20347be46df255a3965ffaf5d0dd08ab21c77c2e07a9736611bfeb`
ran `NemotronNemoBatchWorker` against an exact 30-second track, returned a
bounded `en-US` result, and also tore down cleanly. The private receipts remain
outside Git. These source-exact integration smokes do not prove the full
reference comparison, representative locale/duration matrix, percentile or
resource ceilings, failure/capacity behavior, or rollback, so the frozen
provider checkboxes remain open.

The local duration runner was consumed by the focused target-client gate at
exact executable head `74322bf42c22058ffa88620f28cd4bf118ad8c01`. It accepts
one explicit, hash-bound functional profile. `short-boundaries` streams the
plan's exact 250-ms, 500-ms, 750-ms, one-second, 1.12-second,
two/five/ten/30-second cases in ten-millisecond frames through the real bounded
local-ASR adapter, one live worker, and finalization.
`complete-local-duration-ladders` retains the additional 30-second-through-two-
hour cases for later release qualification. The companion
`local_stream_duration_suite` builder validates and hashes the same public plan,
decodes and hashes a caller-supplied set of vetted private PCM16 sources once,
rechecks raw source identity before publication, and atomically publishes only
the selected immutable tracks plus a versioned `suite.json` beneath external
`YAP_EVAL_CACHE/runtime-tracks`. Its output provides the profile, suite path,
and out-of-band SHA required by the ignored native gate; neither the builder nor
the gate emits source paths or transcripts into checked evidence.

The runtime-plan validator now fails closed on duplicate ladder, boundary, or
metric entries; altered pacing/evidence/unit semantics; and the complete shape
of every provider load cell rather than only the cancellation/capacity subset.
This is contract hardening, not consumed runtime evidence, so it does not change
the Phase 6 score.

The runner fails closed on a full `YAP_CHECKED_HEAD`, the public plan hash, an
out-of-band-pinned private suite, every track manifest, raw WAV identity, and
decoded PCM identity; it emits no transcript or path. Its truthful boundary is
prepared audio frame to final, not physical microphone to final. Focused
contract/lifecycle tests pass. The checked-head `short-boundaries` execution and
current-host no-server microphone/UI/resource gate passed with zero dropped
frames and complete teardown, so the duration checkbox and ADR score may now
reflect that evidence. A focused connected
diagnostic at exact clean head
`a7ff69edf6a2138d15c69986cdf17ea2013ac0ad` used the locked CC-BY-4.0
LibriSpeech fixture and passed all nine cases with zero dropped frames. The
250/500/750-ms cases completed blank; every speech-bearing one-second-through-
30-second case produced text. The strengthened private suite now requires those
six text-bearing outcomes without retaining transcript content.

The provider qualification implementation now builds immutable exact-duration
tracks and hash-bound provider jobs only in a private external cache, executes
synchronized bounded waves, classifies typed cancellation/capacity outcomes,
and emits aggregate p50/p95/p99/throughput/queue/inference/batch evidence without
paths, request IDs, transcript text, or transcript hashes. Resident-provider
HTTP 429 responses now remain typed through durable completion as retryable
`SERVER_BUSY` rather than being flattened into a generic worker failure. The
committed catalog continues to omit Nemotron; an end-to-end candidate-catalog
run requires a matching explicit lock outside the repository.

The provider input builder now derives a single 18-track immutable suite from
the machine plan rather than relying on an operator-assembled manifest list. It
deduplicates the finalized-utterance and batch ladders plus every resident
standard/cancellation/capacity load duration, adds the exact four-hour boundary,
and binds the plus-one rejection boundary, ordered requirement provenance, plan
hash, and every track-manifest hash in private `suite.json`. Focused tests prove
the selection and privacy-safe manifest contract. Building these inputs is not
runtime evidence and does not consume the frozen gate.

The five provider workload CLIs now require that suite plus its out-of-band
SHA-256 instead of accepting an operator-assembled list of track manifests. The
loader performs bounded duplicate-key-safe reads, requires the private real
cache/collection boundary, matches the exact current plan and ordered
requirement provenance, rejects unexpected collection entries, and admits only
the durations used by the selected cell. Each aggregate binds the suite, plan,
and selected duration identities before the checked-candidate hash is computed.
The admitted tracks are handed directly to the workload runner rather than
hashed a second time at runner entry, and the suite plus selected audio are
re-read before evidence publication. Focused tests cover full and cell-specific
admission, pre-publication read-back, and wrong-digest failure. Executable
candidate `aa3268d73ae9a811e84534387c8399ced2cc07e1` built and independently
reloaded one private 18-track suite from the two license-vetted AMI prepared
views, then reverified both checked model directories without launching either
service. That is input preparation only; no runtime cell or frozen provider gate
was consumed.

The standard runner can now select one or more existing plan concurrencies and,
only when exactly one is explicit, repeat that cell up to a fixed bound. This
makes each schema-5 c8/1,600 resource profile executable as eight repetitions of
its unchanged 200-request short-tail cell. Evidence records selected
concurrencies, repeat count, per-run repetition, and the aggregate completed
request count. Focused tests cover valid selection and fail-closed unplanned or
unbounded requests; no GB10 workload was consumed.

The exact-duration entry point is also executable but unconsumed. It selects
only a plan-owned unpaced ladder for the matching resident provider, permits the
exact four-hour boundary only with `batch-file`, and executes every selected
duration once at c1 through the normal checked worker contract. Its aggregate
binds the clean head, serving lock, plan, suite, and selected audio before and
after execution, reports provider metrics, and explicitly declares
`qualificationScope: duration-transport-and-lifecycle` plus
`representativeAccuracyClaim: false`. It cannot substitute repeated-looped audio
for sentinel integrity, representative quality, concurrency, or provider
promotion evidence.

The standard resident-load runner is now executable from the locked plan and
requires every exact-duration track and result beneath the real private
`YAP_EVAL_CACHE`. Its public-safe aggregate records an explicit overall and
per-run expectation decision and continues to refuse specialized cells. Three
separate runners now implement those semantics: cancellation requires target
dispatch, at least two active provider requests, a typed acknowledgement, no
target result, sibling completion, idle read-back, and immediate recovery;
vLLM additionally distinguishes the pinned external-disconnect abort shape,
a counted `finished_reason=abort`, provider completion after cancellation, and
ambiguous accounting. Capacity treats vLLM's `--max-num-seqs 8` as a
scheduler limit rather than a 429 contract and tests the real Yap owner: eight
running plus eight queued slots and the aggregate four-hour PCM reservation,
with retry after drain. NeMo separately proves eight authenticated active
requests, one typed service 429, and retry. The fixed/automatic NeMo contract runner uses the
same locked 30-second source at c1 and c8, requiring fixed `en-US` without
dynamic fields and automatic `auto` with complete detected `en-US` source-span
evidence. Lexical and exact rendered-text parity remain diagnostics for later
provider promotion, not Phase 6 request-lifecycle conditions. Focused portable
tests cover these runners, and their exact-head GB10 candidate-safety executions
passed at `a21964c19e56648e9fddcb5200de419e59a7687c`; promotion decisions remain
Phase 8 work.

All five provider workload entry points plus the resource-qualification entry
point now require the full candidate Git SHA and absolute repository root. A
shared guard admits only the exact clean worktree, hash-binds the runtime plan
and selected provider-serving lock, then repeats both Git-state and input reads
before any evidence file is published. The final candidate envelope replaces
and recomputes the aggregate evidence hash. Focused tests cover clean admission,
dirty/head/input drift rejection, the complete binding, and every entry point.
This closes stale-development-evidence reuse in the runner contract; it does not
attest the separately launched image or consume any GB10 workload.

Focused dirty-head GB10 duration controls completed 30-second c2 plus c1
2-minute, 15-minute, two-hour, and exact four-hour repeated-fixture inputs for
both candidates. The four-hour vLLM control took 49,896 ms with 4.272 GiB
current usage under 32 GiB. The four-hour NeMo control took 279,865 ms with
275,702 ms inference, 2,688 ms accumulated queue time, and 2.356 GiB current
usage under 96 GiB. All runs published bounded results and left their containers
and listeners absent. These are duration/lifecycle controls, not natural
long-form quality, sentinel-integrity, frozen percentile, or promotion evidence.
Those provider-selection questions remain open for Phase 8 rather than blocking
the Phase 6 candidate-safety boundary.

That wall-time comparison is execution-shape evidence rather than a general
model ranking. Yap gives vLLM one offline API request; vLLM splits long audio
into bounded engine requests that it may schedule concurrently. A focused
120-second API control produced four engine observations. NeMo advances the
cache-aware checkpoint through 1.12-second frames across bounded finalized
windows, and the current service exposes no partial transcript to the caller.
The focused result therefore favors Cohere/vLLM for offline long-form throughput
without proving or disproving the separate short/finalized-utterance and future
live-streaming gates. vLLM queue/inference histograms are labeled as engine-
request measurements, while Yap p50/p95/p99 remains API-request wall latency.

A later focused current-source semantic pass exercised the specialized runners
against the resident services. Cohere cancellation was acknowledged in 19 ms;
the sibling and recovery completed, the cancelled result stayed absent, and
the pinned vLLM counters retained one completed stop because its external abort
path does not add the freed request to finished-request metrics. Yap admitted
exactly 16 slot reservations plus a retryable seventeenth rejection, and its
aggregate PCM boundary admitted two two-hour reservations plus a retryable
third rejection; both rejected requests completed after drain. NeMo cancelled
one of two active requests in 885 ms, preserved its sibling, recovered, admitted
exactly eight concurrent 15-minute requests, returned typed busy for the ninth,
and completed that request after drain. The fixed and automatic NeMo routes
used the same source and model at c1/c8: all eight word sequences and all eight
language contracts matched in each mode, while exact rendered text differed
only in deterministic casing/punctuation. Every focused container and listener
was absent afterward. These are dirty-head behavior controls, not frozen
quality, percentile, memory-slope, or promotion evidence.

A sustained exact-input Cohere control then explained the two transcript
identities reported by the 200-request tail run. The authenticated adapter
returned the same 92 case-folded lexical tokens in every request; 189 renderings
contained four commas that the other 11 omitted. A second run with vLLM's V1
engine subprocess disabled reproduced the same 189/11 split at the same
ordinals. The pinned `VLLM_BATCH_INVARIANT=1` path still produced two renderings
and was not adopted. Exact source inspection confirmed greedy FP32-logit
`argmax`, making a request seed irrelevant. The standard load runner now reports
exact and lexical identities per duration and fails word drift, while the
representative corpus gate retains independent punctuation scoring. Raw text,
hashes, and diagnostic logs remain in the private external evidence root.

A focused resource investigation then exercised four consecutive 400-request
c8 runs in each resident process. Cohere vLLM completed all 1,600 requests at
303.7 audio-seconds/second on the first repeat and 321.3-322.4 when warm. Its
tail cgroup median changed by less than 0.5 MiB, entrypoint virtual-data median
by less than 0.7 MiB, cgroup current stayed below 3.12 GiB, cgroup peak stayed
below 5.75 GiB, and memory-event deltas were zero. Every repeat retained one
lexical identity while reporting the known two exact punctuation renderings.

Resident NeMo completed all 1,600 requests at 268.9-274.2 audio-seconds/second
with one exact and lexical identity. CUDA allocation remained about 1,296 MiB
(1,312 MiB maximum) and reservation remained exactly 4,956 MiB. Cgroup current
stayed below 3.63 GiB and peak below 6.04 GiB. Anonymous RSS showed a GB10
unified-memory residency/reclaim sawtooth, but the entrypoint allocation extent
was fixed at about 11.70 GiB and its last-half median grew by only 8 KiB. A
private diagnostic collected Python cycles and called `malloc_trim(0)` between
runs; neither released material RSS, ruling out a Python-object or ordinary
glibc free-list explanation. Stream/state maps were empty on terminal paths and
CUDA counters were flat. The service now reuses a bounded HTTP worker pool
instead of creating one native thread per request; the same load retained model
admission/cancellation semantics and reduced warm median queue time from about
24 ms to 3-5 ms.

These focused results selected, but did not consume, the frozen resource
contracts in runtime-plan schema 5. Both profiles require c8/1,600 completions,
at least 60 seconds and 200 samples in the tail, no memory-event increments,
at most 256 cgroup tasks, and no more than 64 MiB absolute tail growth in the
entrypoint allocation extent. vLLM is capped at 4 GiB current, 8 GiB peak,
5 GiB entrypoint virtual data, and 128 entrypoint threads. NeMo is capped at
5 GiB current, 8 GiB peak, 14 GiB entrypoint virtual data, and 256 entrypoint
threads. Physical RSS slope remains reported rather than hidden, but GB10
residency oscillation cannot substitute for growing allocation extent. The
one-time checked-head resource qualification and representative-quality gates
remain open, and all raw samples stayed in the private external cache.

The current-source enforcement rerun then passed all eleven checks in both
profiles. vLLM completed 1,600/1,600 with a 75.2-second/286-sample tail,
3.12/5.75 GiB maximum current/peak cgroup memory, 4.29 GiB maximum entrypoint
allocation extent, 0.91 MiB tail allocation growth, 224 cgroup tasks, 86
entrypoint threads, and zero memory-event increments. NeMo completed
1,600/1,600 with an 87.5-second/334-sample tail, 3.65/6.04 GiB maximum current/
peak cgroup memory, 11.71 GiB maximum entrypoint allocation extent, zero tail
allocation growth, 222 cgroup tasks and entrypoint threads, stable CUDA
counters, and zero memory-event increments. Both containers and listeners were
absent afterward. This is focused contract enforcement, not the one-time frozen
Phase 6 run.

The reproducible resource path now includes a checked-in cgroup-v2 sampler rather
than depending on the earlier manual collection helper. It resolves only the two
Yap-owned checked-head container names, rejects root or moved processes, reads
the unified cgroup and entrypoint counters through bounded real files, and closes
one private JSONL series through explicit workload start/end/stop markers. The
existing strict observation qualifier round-trips that schema. Focused portable
tests pass; no resident model or frozen resource profile was run by this change.

The resident-service lifecycle composition is executable and has now passed at
one exact clean GB10 head. The wrapper verifies the two already-present model
directories, builds exact-head ARM64 images, creates a temporary checked
internal Docker bridge, runs each container without a Docker-published port or
external egress, owns a bounded numeric-loopback proxy, and runs vLLM then NeMo sequentially. Typed startup
unavailability may retry; wrong authentication, runtime version, model identity,
or malformed readiness fails immediately. The wrapper consumes the plan-owned
duration, candidate-safety load, cancellation, capacity, language-contract, and
c8/1,600 resource cells, then removes the provider and network before
finalization. The Cohere candidate-safety set deliberately excludes the retained
`vllm-long-waves` and `vllm-mixed-eight` provider-promotion comparators. Its
aggregate rejects an incomplete concurrency set, omitted exact four-hour batch
boundary, changed suite/head, failed child, unclean launcher exit, or retained
container, proxy, launcher, listener, or network. All raw inputs/logs/samples/snapshots remain in the private
external cache.

The first checked-head attempt at
`e7d322fc07c6e1a39e69c2eec4d45e2c94d79e3a` passed Cohere readiness, the c1
duration ladder, and short-tail c1/c2/c4 isolation, then failed closed before
NeMo when four completed c2 copies of the same accuracy-ineligible 15-minute
AMI-derived runtime control produced materially different transcripts. Pairwise
normalized edit ratios were 6.9% through 21.6%; the four results differed from
the c1 transcript by 22.4% through 23.4%. Exact cleanup passed. That private
receipt is retained as negative long-form Cohere promotion evidence rather than
as a candidate-safety blocker for a provider that Phase 8 may replace with
Tiron. It points to provider/model stability and does not, alone, prove the
absence of cross-request mixing. A corrected exact-head GB10 run remained open
at that point;
no representative-quality decision, promotion, or complete one-time Phase 6
matrix has passed. Container cgroup samples exclude the small
host proxy process group; request wall latency includes it, and whole-host
capacity remains a later system gate.

The following exact-head attempt at
`5b56929889925c933c63374a8d7ab282b6b82a3f` progressed through every Cohere
candidate-safety child before the resource finalizer. All 1,600 c8 requests
completed; current, peak, allocation, task, thread, and memory-event checks
passed. The run failed because a 116.317-second workload yielded a 57.798-second
last-half tail below the frozen 60-second coverage and because a resource-only
repeat reported three lexical identities. Exact teardown passed and NeMo did not
start. The correction does not lower thresholds: resource evidence now uses an
explicit `resource-lifecycle` scope that reports lexical variance without making
it a model-promotion condition, and the observation stays open for at least 125
seconds. The new exact-head rerun remained open at that point.

Exact head `63318e51d569a1851f1a6daf8d1b707c353f2fa8` then passed the full
corrected Cohere lifecycle and sequential teardown. NeMo reached exact-model
readiness, but its finalized-duration aggregate reported only 2/9 completions
even though all nine worker result files existed: seven short/silent cases
returned the canonical empty transcript allowed by Yap's production result
contract, and the generic observation harness had treated those valid results
as failures. Exact
cleanup passed. Duration-transport now accepts published canonical empty text;
provider-behavior, request-lifecycle, and resource-lifecycle scopes still
require non-empty speech-fixture output. A new exact-head run remained open at
that point, and no provider was promoted.

Exact head `4ffec120f212d20a26e314108940989c1b6e93a5` passed Cohere's complete
corrected lifecycle and exact teardown, then passed NeMo readiness and both
duration ladders through the exact four-hour transport boundary. NeMo's
short-tail c1/c2/c4 cell completed and published all 600 non-empty results. One
lexical output appeared 198 times at c4 and a one-word variant appeared twice;
c1 and c2 each produced one lexical identity. All result envelopes remained
bound to the originating job, input-audio hash, model lock, and result path, and
cleanup was exact. That repeated-single-input cell cannot prove cross-request
mixing; its failed predicate was model-output determinism. The Phase 6 wrapper
therefore uses a distinct `request-lifecycle` scope for standard short/long
cells: it keeps identity, result publication, non-empty speech output,
completion, provider-idle, concurrency, and teardown requirements while merely
reporting lexical variance. `provider-behavior` remains available for Phase 8's
Tiron comparison. A new exact-head run remained open at that point, and neither
provider was promoted.

Exact head `27108e1f591920b5a62496f988ae9ee7b335f2ce` passed every Cohere child and
exact teardown. NeMo then passed readiness, both duration ladders, all 600
short-tail results, and all four 15-minute request-lifecycle results. The latter
reported three lexical identities without losing any identity-rich result.
Across c1/c8 the subsequent fixed and automatic paths each completed 16 results,
and every automatic result carried detected `en-US` source-time evidence. None
matched the fixed path lexically because automatic segmentation changed wording.
The specialized checker had embedded lexical equality inside language-contract
conformance, so it failed and cleaned up before later NeMo cells.

The corrected plan cell is `nemo-finalized-fixed-auto-contract` with
`fixed-and-auto-language-contract` semantics. It requires all fixed/automatic
job, audio, model, language, segment, and span contracts while recording lexical
and exact-text parity with `lexicalParityRequired: false`. Provider quality stays
in Phase 8's Tiron comparison. The changed plan hash requires a new private
suite envelope and another exact-head lifecycle run.

Exact head `2b9118ead1df1f3220da65846c2aa8949d90d83d` then consumed the new
18-track suite and passed all Cohere children plus nine of ten NeMo children.
The NeMo c8 resource workload completed 1,600/1,600 and passed memory,
allocation, timing, sampling, and memory-event controls, but its resource
aggregate failed at 262 tasks/entrypoint threads against the frozen 256 ceiling.
A fresh ready process used 63 tasks and an isolated c8 wave reached 222; the
full lifecycle entered the last cell at 224, then created 38 native-library
waiters in its first sample. Teardown was exact and no aggregate published.

Replaying the finalizer independently exposed a second stale contract before a
future successful resource child could publish: the mixed long-window child
correctly selected both 30 seconds and 15 minutes, while the finalizer expected
only 15 minutes. It now requires both members and rejects either partial suite.
The server image now caps BLAS/OpenMP/Rayon and PyTorch pools at eight, and the
service derives 18 HTTP workers from the eight-active-request contract instead
of keeping 32. Exact head `17a727f272943e6bc57a4253247e7e824855c086`
passed the focused correction: c8/200 completed in 18.930 seconds at 316.957
audio-seconds/second, then the unchanged c8/1,600 resource qualifier passed all
eleven checks over 147.583 seconds. Fresh readiness used 34 tasks and both
workloads peaked at 97 tasks/entrypoint threads; current/peak cgroup memory stayed
near 3.61/6.03 GiB, CPU averaged 1.013 cores, memory-event deltas were zero, and
teardown was exact. The 256 ceiling was not raised. A complete sequential
exact-head lifecycle run still remained required at that point, and neither
provider was promoted.

Exact head `a21964c19e56648e9fddcb5200de419e59a7687c` then passed the complete
sequential candidate-safety lifecycle against the plan-bound 18-track suite.
All 18 child artifacts passed and the bounded aggregate published as evidence
SHA `a6931acc127f2ca74e6d3a4c8c9aa6c93e33289f1d1312a4626d659dfcbeb9cb`.
The run covered Cohere duration, c1/c2/c4 short-tail isolation, cancellation,
slot/PCM admission, and c8/1,600 resources, then tore Cohere down before starting
NeMo. NeMo covered finalized and batch duration ladders through the exact four-
hour transport boundary, c1/c2/c4 short-tail and 15-minute request lifecycle,
fixed/automatic language contracts at c1/c8, mixed 30-second/15-minute long
windows, cancellation, active capacity, and c8/1,600 resources. The final NeMo
resource cell reached 105 tasks/entrypoint threads against unchanged 256
ceilings, recorded zero memory events, and passed all frozen memory/tail checks.
Finalization and an independent read-back found no provider container, temporary
network, runtime process, proxy/listener, or ports 18000/18001. This consumes the
Phase 6 resident-provider component only; broad quality, output determinism,
rollback, and Cohere-versus-Tiron promotion evidence remain Phase 8 work.

A focused dirty-head GB10 proof now also exercises the actual resident Cohere
worker through vLLM's authenticated loopback transcription API. Exact readiness
passed, the public fixture matched the Transformers reference transcript hash
with zero normalized word errors, and independent c2/c4/c8 waves completed in
145/370/303 ms. Observed container memory was 3.09 GiB. An initial 1,892-ms
cancellation result exposed a Yap client interruption bug rather than a vLLM
limitation: cross-thread `HTTPConnection.close()` did not wake the blocked Linux
response read. The corrected probe waited for an active vLLM engine request;
explicit socket shutdown produced an 18-ms acknowledgement, an engine-abort log,
and a zero-running-request read-back. No cancelled result was published, 104-ms
recovery preserved the reference hash, and teardown removed the container and
loopback listener. Seventy-one affected local provider tests passed with one
expected platform skip. This is focused implementation evidence only; the frozen
representative duration, percentile, memory-slope, admission, and reference
comparison items above remain unchecked.

A focused dirty-head GB10 proof now exercises the actual resident worker and
authenticated adapter rather than calling the model directly. The pinned
`.nemo` checkpoint restored in about 29.5 seconds. Eight simultaneous fixed/auto
jobs completed in 1,150 ms and formed a model batch of eight; fixed `en-US`
matched all 23 normalized golden words with one punctuation difference, while
automatic mode matched the exact public golden transcript after locale tags
were excluded from spoken text. Cancellation was acknowledged in 13 ms without
result publication, immediate recovery succeeded, and the owned container and
loopback port were absent afterward. Focused local native/service/runtime/infra
coverage then passed 68 tests with two expected platform skips. This result also records a
pinned-source correction: the exercised unified NeMo wrapper accepted but did
not apply prompt vectors, so Yap validates the checkpoint prompt catalog and
applies the selected projection after the encoder and before RNNT decode.

This is implementation evidence only. It does not consume the one-time Phase 6
matrix, qualify representative locales or durations, prove persistent mixed-
user production capacity, or promote the candidate.

A private focused natural-speech intake screen then selected one
original-language European Parliament speech for each of 19 post-freeze
languages, removed the exact roughly 60-second media envelope, and ran every
derived 16-kHz mono WAV through the desktop's real Silero preprocessing path.
Increasing Sherpa's result ring to 120 seconds removed allocator growth warnings
without changing the preprocessing-evidence hash. Cohere/vLLM completed its nine
supported locales/711 audio seconds at c8 in 6,919 ms model-ready wall time;
NeMo initially failed the same Czech, Danish, British-English, and Estonian
requests at both c8 and c1. A transcript-free private diagnostic proved that
the selected prompts were correct but the model sometimes emitted another
valid locale tag. Yap had incorrectly treated that non-authoritative metadata
as a fixed-route failure. The corrected policy keeps the fixed prompt immutable,
strips known tags, and leaves tag evidence to explicit automatic mode. Twenty-
seven focused NeMo tests passed, and the c8 rerun completed all 18 supported
locales/1,380 audio seconds in 9,024 ms, formed a real batch of eight, published
18 unique results, and recorded no failure or busy outcome. Both runs cleaned
their provider container and loopback listener.

The upstream Parliament text is revised and has not received the required two
independent listens or adjudication. The 15.86% Cohere and 22.06% NeMo aggregate
normalized WER values cover different locale sets and remain explicitly
promotion-ineligible diagnostics. No audio, reference, transcript, per-case
identity, or raw diagnostic entered Git. This closes an implementation defect
and adds representative intake evidence; it does not check the frozen provider,
locale, or complete Phase 6 gates above.
The executable review/registry contract described above is now ready to admit
those private artifacts, but it cannot manufacture them and does not convert
this screen into gold or promotion evidence.
On 2026-07-22 the complete 245-file intake/evidence tree was copied out of
volatile storage into an owner-only persistent external cache, then rehashed as
a tree with an exact source/copy match. No private path, digest, audio,
reference, hypothesis, or raw receipt entered Git. This preservation step does
not satisfy the still-missing independent listens, adjudication, attribution,
or registry decision.
A separate owner-only seven-file Spanish review-source packet now contains only
the canonical audio, upstream reference, source/derivation/preprocessing
receipts, and a byte/hash inventory. Its manifest explicitly excludes model
hypotheses, peer reviews, and provider scores and leaves the exact locale at
`requiresHumanAdjudication`. It is ready for a later blind assignment, but it is
not a listener receipt, an adjudicated reference, or promotion evidence.

At exact executable source SHA
`2caf1969000154ffba24511a5c35b57f7f975036`, a focused natural long-meeting
comparison ran both complete 17.49-minute AMI `ES2004a` conditions through the
desktop production normalizer and Silero VAD, reverified all client chunks and
canonical PCM through server input preparation, and built 37 contiguous
utterance windows per condition. Cohere/vLLM completed the close/far conditions
in 8.615/4.473 seconds with 46.250%/42.367% normalized WER and
25.496%/29.021% punctuation F1. NeMo/Nemotron completed in 18.065/16.858
seconds with 26.046%/37.919% normalized WER and 11.621%/10.191% punctuation
F1. This one comparator therefore favors Nemotron lexical accuracy and Cohere
throughput/punctuation; it invalidates a universal accuracy label without
promoting either provider.

The AMI reference is public, exposure-unknown, known-defective, flat-ordered
across overlapping speakers, and not independently reviewed. Neither route
returned timestamps or speaker identity in this run. References, hypotheses,
per-case output, host paths, and raw receipts remain in the private evaluation
root. Cohere's published 8.15% Open ASR AMI result is a different short-form
protocol: immutable dataset revision
`470b2948906c624f828a7349d92b92ec80e84fe0` contains 12,643 sorted
individual-headset utterances no longer than 26.2 seconds, and upstream runner
`b6117f86f73edbca3b5dfc9960d0eb65d685258e` evaluates them in batches of 64
with fixed English normalization. It does not exercise a complete mixed or
far-field meeting, cross-speaker overlap ordering, or Yap's long-form input
preparation. Replaying that public set would remain comparator-only evidence
and is therefore not inserted into the frozen promotion gate. Both containers
and listeners were absent afterward. The vLLM engine
completed shutdown but its pinned upstream Torch/vLLM process emitted a
weakref-time `UnicodeDecodeError` and one leaked-semaphore warning during
interpreter exit. The same finalizer traceback reproduced with the pinned vLLM
CLI before a model or Yap request and matched PyTorch upstream fix
`c5f8ebc91a8727a9056734f73329c217328b8989`. Exact executable commit
`da9f7682d6337df0d1bfb26e069781d8a64ec726` now applies that exact
BSD-3-Clause backport at image build time with pinned-source assertions. A
source-exact ARM64 no-device reproducer and a separate real-model/public-fixture
lifecycle both omitted the finalizer trace; the real lifecycle also omitted the
semaphore warning, exited zero after observed engine/API shutdown, and removed
its container/listener. The real run verified all model artifact hashes,
returned zero normalized public-fixture word errors, and observed 2.968 GiB
container usage. Private log/summary hashes are recorded in ADR 0025. This
focused correction does not close the provider, meeting, locale, duration, or
complete Phase 6 gates; the frozen exact candidate must repeat log-clean
lifecycle assertions.

#### Retired Triton experiment — historical negative evidence

The following private dirty-development record explains why ADR 0025 retired
the common Triton plane. It is retained for audit and must not be read as the
current executable plan, runtime matrix, or promotion target. The public runtime
plan now contains Cohere Transformers/vLLM and Nemotron Transformers/NeMo routes.

Focused non-gate GB10 evidence on 2026-07-17 used development image
`sha256:d88e92e3c6a81b28fe90a89f257d565a6b06c9e1479f1a929fc4a99ec074db52`
labeled `4e36c3199ea9-dirty-r13`; it is not checked-head promotion evidence. A
mixed eight-request short/300-second route probe preserved four distinct route/
duration identities and exact server counters, but only two of four compatible
pairs actually batched. A synchronized 200-request c8 short burst completed in
12.203 seconds at 121.855 audio-seconds/second with exact fixture parity and
mean observed batch size 2.667. Its release-to-completion p50/p95/p99 were
6.609/11.812/12.203 seconds and are descriptive burst statistics, not steady-
state or end-to-end service SLO evidence. No batch-of-eight execution occurred;
the current queue policy still requires the frozen Perf Analyzer and capacity
gate. A separate exact 480,000-sample (30.000-second) c1 runtime control
completed in 547 ms wall time with 439.517 ms Triton inference and 2.164 ms
server queue. The runner correctly emitted null WER, zero accuracy-sample
increment, and independent-promotion ineligibility. That run proves the private
exact-duration seam only; it is not transcript-quality, concurrency, or
promotion evidence.

The corrected synchronized-wave harness then compared 200 copies of the same
exact 30-second runtime control at c1 and c8. At c1, the 2-ms policy recorded
p95/p99 531/532 ms and 58.796 audio-seconds/second; the 10-ms challenger
recorded 532/547 ms and 57.692 audio-seconds/second. At c8, 2 ms recorded
p95/p99 1,640/1,703 ms and 162.092 audio-seconds/second, while 10 ms recorded
1,594/1,672 ms and 164.240 audio-seconds/second. The 1.3% c8 throughput change
was not a material improvement and accompanied a 1.9% c1 throughput regression,
so the 10-ms challenger was rejected and the executable policy remains 2 ms.
This remains dirty development evidence, not a checked-head capacity claim.

Adversarial review invalidated the earlier 2-ms and 10-ms cancellation evidence:
the original probe could start cancellation before confirming active dispatch
and derived acknowledgement time from the configured delay instead of the
actual intent timestamp. The corrected transport state machine records the
timer callback's monotonic intent time, distinguishes a cancellation callback
from an already completed normal result, and rejects incoherent isolated model
counters. Neither exploratory file qualifies the final candidate.

The first corrected 2-ms rerun confirmed two dispatches, measured 31 ms from
actual intent to acknowledgement, and produced two successful singleton
inferences plus one failed pre-inference cancellation. The sibling and recovery
completed in 297/94 ms, so that run proved only bounded cancellation and
recovery. A later uncontrolled eight-request wave put all seven siblings in a
batch of seven but inferred the cancelled request as a singleton. The probe
correctly kept sibling isolation false.

The generalized probe now uses a post-preparation release gate and derives the
cancelled request's batch membership only from an isolated server-member minus
returned-response multiset. A 100-ms negative control released one sibling
leader followed by the seven-request follower wave; it recorded one singleton
leader, six batch-of-six siblings, one failed cancellation before inference,
and one singleton recovery, so membership remained unknown and the claim stayed
false. The 500-ms focused rerun recorded one singleton leader, six surviving
siblings from a batch of seven, and one singleton recovery. The unique missing
batch-of-seven member was the cancelled request. All six same-batch siblings
completed, client cancellation was acknowledged in 47 ms, and recovery
completed in 78 ms. Exact counters were nine inferences, three executions,
nine server successes, zero server failures, and batch executions `{1: 2,
7: 1}`. Because Triton recorded the cancelled member as a server success, this
proves client cancellation isolation and continued service availability, not
that backend or GPU work stopped.

All of those files remain private dirty-development diagnostics rather than
checked-head promotion evidence. The same-batch property must rerun on the
frozen Phase 6 candidate. An initial post-tensor two-request duration control
still produced five singleton executions and correctly remained inconclusive.
The replacement control uses the workload shape the retained 2-ms scheduler can
actually batch: one short leader followed by a seven-request wave containing
three exact 262,144-sample and four exact 524,287-sample inputs. The focused run
recorded a singleton leader plus one mixed-duration batch of seven; the exact
524,287/524,288 boundary pair remained two singletons. Per-wave counters were
`{1: 1, 7: 1}` and `{1: 2}`, and total counters were eleven inferences, five
executions, and `{1: 4, 7: 1}`. Boundary separation is therefore proved for the
dirty candidate. The short control increased from 297 ms solo to 1,500 ms in the
mixed wave, a 1,203-ms head-of-line penalty. That penalty is disclosed offline-
batch evidence, not an accepted SLO or proof the power-of-two bucket is optimal;
the frozen mixed-load gate must decide promotion or further bucket refinement.

A focused current-source GB10 proof on 2026-07-18 then exercised Nemotron
revision `f3d333391852ba876df169dcc9ba902d25b6ab0b` through both fixed `en-US`
and dynamic `und` routes. The source overlay was labeled
`4e36c3199ea9ee8d74932220504ad7391a17461a-dirty-phase6-review1`; it used
Python 3.12.3, NVIDIA Torch `2.13.0a0+8145d630e8.nv26.06`, CUDA 13.3, BF16,
and the exact locked overlay packages. Both runs preserved the complete-source
utterance-plan identity and passed the private parity harness's exact
authoritative transcript contract plus stable runtime-identity comparison. The
dynamic run also preserved the exact ordered language-segment hash. It
predated the versioned source-span envelope, so the frozen checked-head rerun
must additionally preserve the exact `languageSpanEvidence` hash. Transcript
text and raw results remain outside Git under the private evaluation boundary.

The Triton server ran non-root with no network, no published ports, a read-only
root, all capabilities dropped, and `no-new-privileges`; the parity client used
only the server container's isolated loopback namespace. Both model endpoints
were ready. The separate focused-only client overlay used the `uv.lock`-pinned
Triton 2.70.0 and ARM64 python-rapidjson 1.23 wheels after exact SHA-256
verification and an offline build. Teardown took 6,409 ms and left no owned
container or matching GPU process. The measured candidate calls were warm while
the standalone references included independent model startup, so their timings
are deliberately not treated as a performance comparison. This is private,
dirty-development correctness and containment evidence only; the unchecked
frozen parity, workload, capacity, and promotion gates remain unchanged.

A focused current-source rerun on 2026-07-20 closed the source-span gap without
consuming the frozen gate. Server image
`sha256:a5f3afad18f4c66abfbbdb7a4abf402d37082ca665f062d1a11ce82b5915f666`
and client overlay
`sha256:90b4dee2b446372635d84b766c26fb3f24bb7624b38d3848b4c92cff035e4e2d`
were both ARM64 and labeled
`4e36c3199ea9ee8d74932220504ad7391a17461a-dirty-focused`. Independent
references and warm Triton calls matched the exact authoritative contract for
Cohere fixed English, Nemotron fixed `en-US`, and Nemotron dynamic `und`. The
dynamic route preserved the exact complete-source utterance-plan, ordered
language-segment, and `languageSpanEvidence` identities; the fixed routes
correctly emitted no dynamic evidence. Transcript text and raw results stayed
in the restricted private workspace. The server had no published ports and
used an internal Docker network; the production client retained its numeric-
loopback-only endpoint by sharing that container's network namespace. Exact
teardown left zero matching server containers, client containers, or internal
networks. Timings remain descriptive because the references include independent
startup while the candidate calls are warm. This is still dirty-development
parity evidence, so the frozen checked-head parity checkbox remains open.

A focused July 21 exact-duration comparison then exposed a correctness boundary
that the singleton parity probe could not see. Using the same dirty current-
source server and client images, 200 repetitions of one exact 30-second runtime
control at Cohere c2 formed 100 batches of two, kept one authoritative output
identity, measured 631/638/641 ms p50/p95/p99, and processed 94.579 audio-
seconds/second. At c4, Triton formed three singleton, 94 batch-of-two, and three
batch-of-three executions. The three singleton results exactly matched an
independent transient-worker result; every batched result was internally stable
but differed from that reference by four word substitutions. The c4 run
therefore had two authoritative output identities and fails exact batching
parity even though its descriptive throughput was 95.134 audio-seconds/second.

A deliberately small Nemotron control avoided spending a full p99 run after the
Cohere failure. Triton grouped eight requests into c2/c4 scheduler batches, but
every result reported engine batch size one because the checked backend executes
recordings serially. Throughput stayed 92.095/92.593 audio-seconds/second while
p50 doubled from 649 to 1,287 ms. With both models resident, the server cgroup
recorded 5,943,128,064 current and 7,379,800,064 peak bytes; teardown took 6,452
ms and left no owned container, network, or matching GPU process. Raw results,
transcripts, and resource output remain private. The public harness now hashes
the full repeated-input authoritative output, returns a failing CLI status on
drift, and includes alignment in exact reference parity. That cross-request-
batching configuration is not promotable; any future true-batching successor
must restore exact batched output and show real model-level throughput
improvement before it can replace the singleton-resident profile.

The bounded successor tested later on July 21 removes the unsafe assumption
rather than trying to make cross-request model batches authoritative. Both model
configs use serving profile `single-resident-queued-v1`, one GPU instance,
`max_batch_size: 1`, a maximum queue of 16, zero scheduler delay, and ordered
responses. Server image
`sha256:0644dcf4a6960b9fc8da45eadf66625e171f92ac5726e531a1abe21f9e0d9acb`
and the final focused evidence client
`sha256:5142425745124b6eaae00bd784987759e2cbd2517694135191b8fe536d290b82`
were labeled from dirty development head
`4e36c3199ea9ee8d74932220504ad7391a17461a`; the server had no published
ports, and the client shared only its isolated loopback namespace. Cohere fixed,
Nemotron fixed `en-US`, and Nemotron dynamic `und` each matched the independent
reference's exact authoritative contract. The dynamic result also preserved the
utterance-plan, ordered language-segment, and source-span identities.

One-warmup, eight-request c4 controls kept all Triton executions at batch size
one and one authoritative result identity. Cohere measured 329/661 ms p50/p95,
6.116 requests/second, and 45.474 audio-seconds/second. Nemotron fixed measured
209/415 ms, 9.674 requests/second, and 71.923 audio-seconds/second; Nemotron
dynamic measured 220/432 ms, 9.368 requests/second, and 69.649 audio-seconds/
second. These are synchronized direct-worker warm controls over one 7.4-second
licensed fixture, not end-to-end SLO or frozen baseline-comparison evidence.
With both models resident, `nvidia-smi` attributed 6,490 MiB to the server's GPU
processes; the cgroup reported 9,888,362,496 current and 11,519,070,208 peak
bytes while Docker's simultaneous accounting reported 5.355 GiB. The different
unified-memory accounting boundaries are retained rather than collapsed into a
single memory claim.

A separate queue proof canonicalized three licensed private English fixtures to
16 kHz mono PCM16, completed a 16.38-second leader, cancelled a distinct
7.92-second follower before execution with 20 ms acknowledgement, and completed
a 6.54-second recovery in 132 ms. Triton recorded exactly two singleton
executions and successes plus one separate cancellation failure. The final
teardown took 5,737 ms and left the captured GPU processes and container absent.
Raw audio, transcripts, per-request results, and host paths remain private; the
private evidence manifest has SHA-256
`dab6798a54c9a6b72881a7c4d9b31acff796df9eeae770c989bc9af2c8a830fa`.
This makes the singleton-resident successor a viable frozen-gate candidate; it
does not select Triton, advertise server live, or consume the one-time Phase 6
matrix.

### 8. Publish truthful timing evidence

- [x] Implement the measured Cohere decoder-attention candidate behind an
      English-only capability gate first: ordinary BF16 generation, FP32 Q/K
      reconstruction, finite checks, filtering, monotonic DTW, and exact raw-
      transcript reconciliation.
- [x] Bound word count, token/matrix count, and result size. Reject NaN/Inf,
      non-monotonic, overlapping, out-of-source, missing, or transcript-
      divergent intervals.
- [x] Publish `alignedWords: []` plus a typed unavailable reason when the
      provider/language or evidence fails its gate. Never invent even spacing,
      confidence, or speaker attribution.
- [x] Keep the catalog at `wordAlignment: false` for Phase 6. Defer the
      accuracy/latency/memory promotion workload until a later selected route
      proposes to advertise alignment.
- [x] Retain Qwen3 ForcedAligner only as a later, separately licensed/measured
      challenger for supported languages. No Qwen artifact, runtime, or code is
      part of the Phase 6 product or evidence path.

A contained non-gate GB10 lifecycle proof on 2026-07-18 exercised the current
alignment contract and source in the exact Python 3.12.3, NVIDIA Torch
`2.13.0a0+8145d630e8.nv26.06`, CUDA 13.3, BF16 runtime. Two runs produced WER
0.0, 23 source-bounded words, identical transcript and alignment hashes, and an
unchanged `sdpa` backend. The first/warm wall times were 2,306/159 ms and peak
GPU allocation was 4,190,281,728 bytes. This is focused implementation evidence,
not checked-head promotion evidence; the catalog remains false until the one-
time Phase 6 gate.

### 9. Complete the review UX and contract migration

- [x] Present primary, per-job fixed, suggested, dynamic, unknown, unsupported,
      and timing-unavailable states in plain language.
- [x] Keep detected labels correctable without mutating retained source or a
      previous immutable server result.
- [x] Migrate persisted/job/result contracts with backward-readable Phase 5
      records and fail-closed handling for future/unknown schemas.
- [x] Preserve History ownership and one canonical transcript/review surface.
- [x] Add accessibility coverage for picker keyboard use, labels, focus,
      reduced motion, errors, and non-color-only quality/status communication.

The native completed-result catalog now derives a bounded language/timing
summary only after immutable `result.json` verification. Fixed, automatic per-
segment, unknown-segment, timing-available, timing-unavailable, and legacy-
timing states flow through the existing native History owner into the existing
transcript review surface; no second cache or review owner was added. The
catalog-derived import picker exposes explicit automatic mode only when the
current verified capability advertises it, and guarded LID failures use plain-
language explanations rather than protocol reason codes. Focused Rust and React
tests cover legacy fixed results, dynamic unknown segments, remote-history
projection, and non-color status labels. Dynamic History review now loads a
bounded segment projection lazily and appends separately owned
`language-label-corrections/correction-N.json` revisions. Every revision is
strict-schema, canonical-BCP-47 validated, sequential, hash-chained, and bound
to the exact immutable `result.json`, session, source span, and original label.
Writes use optimistic revision checks plus one native mutation owner; restart,
stale/no-op/noncanonical requests, concurrent writes, unexpected artifacts, and
future correction schemas fail closed. Restore actions append another revision.
Focused tests prove the original `result.json` and `transcript.txt` bytes remain
unchanged, and History derives corrected/needs-review counts only after the
correction chain verifies. The UI renders at most 50 rows at a time.

Focused migration tests also reopen compatible Phase 5 persisted results while
rejecting future desktop and server schema versions without rewriting them.
The implementation naming pass also moved accessibility, LID-container,
model-serving-container, development batch-server, evaluation-metric, correction, and
ledger-preparation code to behavior-owned module/type names. Runtime deployment
configuration now uses `YAP_BATCH_ASR_ENABLED`, `YAP_ASR_*`,
`YAP_BATCH_JOB_STORAGE_DIR`, `YAP_CHECKED_HEAD`, and `YAP_DOCKER_BINARY`, with
the keys centralized in `config/runtime_environment.py`. The remaining
executable paths and callable names now describe their runtime function. New
serialization uses `legacyImplicitEnglishDefault`; `legacyPhase5Default` and
`legacy_phase5_default` remain backward-readable wire/persistence tokens only.
The reusable checked-head tools are named `gb10-asr-runtime-gate`,
`isolated_runtime_evidence`, and `private-server-asr-gate`; the OpenAPI records
functional runtime status/ownership instead of phase-numbered extension keys.
Focused verification passed 99 Python tests plus 80 subtests, five desktop gate
helper tests, TypeScript checking, Rust compatibility tests, and strict Clippy.
Focused accessibility evidence now covers unique labels and error descriptions,
live atomic errors, reduced-motion styling, non-color language/timing status,
keyboard selection and focus return, and a 390-by-760 narrow-window layout. The
narrow proof exposed and closed the fixed navigation/picker clipping defect.
The muted explanatory-text token now measures 4.69:1 against the base
background, above the WCAG AA 4.5:1 normal-text threshold. The complete
full-matrix accessibility rerun remains part of the one-time Phase 6 gate.

### 10. Reconcile evidence and close the exact head

A pre-gate documentation reconciliation on 2026-07-22 checked the current
architecture, Voice OS synthesis, roadmap, ADR index/status, root and server
READMEs, OpenAPI ownership, and server-node runbook against the executing
provider split. It restored ADR 0025 cross-references, amended ADR 0007 with the
implemented fail-closed Cohere timing path, raised only that evidence-backed ADR
implementation score from 10 to 100, and made the superseded Triton research
snapshot explicitly historical. Focused alignment verification passed 29 tests
plus 20 subtests. A 2026-07-23 focused assembled-system run then validated exact
executable head `05d1b82017447df04d46ccc8fa729c5a6a0d0b13` from the Windows
desktop through durable preprocessing, interruption/recovery, the advertised
private-server route, server-authoritative publication, History review, and
remote teardown. It exposed and closed two integration-only blockers: advisory
VAD evidence could incorrectly hold a job in preflight, and the gate expected a
completed job to remain in the recoverable queue instead of following it into
History. Focused follow-up verification passed five desktop gate files with 30
tests, seven Rust client-stage ledger tests, JavaScript syntax checking,
TypeScript checking, and whitespace validation. The later focused target-client
qualification passed at exact head
`74322bf42c22058ffa88620f28cd4bf118ad8c01`; the checklist remains open until
the frozen complete gate determines the final claims and the exact candidate
receives one last read-back.

- [x] Resolve focused correctness, security, privacy, license/provenance,
      maintainability, accessibility, and resource-bound findings.
- [x] Update ADR implementation scores only for behavior proved by executable
      tests and observed runtime evidence.
- [x] Reconcile current architecture, Voice OS synthesis, roadmap, status,
      OpenAPI, runbooks, and this plan with what actually executes. The OpenAPI
      contract remains unchanged because this closure changes only local runtime
      lifecycle and qualification behavior.
- [ ] Freeze one Phase 6 candidate and run the complete applicable local/native/
      server/GB10 matrix exactly once.
- [ ] Open a focused PR, require hosted exact-head CI/CodeQL and any applicable
      disposable-Windows lifecycle evidence, review the checked SHA, and merge
      only that green reviewed head.

Exactly three read-only adversarial reviews covered runtime/concurrency,
architecture/maintainability, and contracts/evidence. Their Phase 6 blockers are
closed: recording language evidence is bound to committed PCM and persistence
loss degrades durably; partial provider startup closes acquired workers; NeMo
shutdown handles a connection factory that never returns; production images
exclude the evaluation package; the model bundle uses a strict resource
allowlist; and runtime/provenance documents match the executable owners. The
focused regression rerun passed 23 Rust tests, 42 server lifecycle/containment
tests with one expected skip, 19 production-image contract tests, and three
model-provenance tests. Large cohesive-module decomposition remains correctly
queued for the separate post-Phase-6 checkpoint and is not a release-correctness
finding.

The Phase 6 merge activates the separately queued
[Codebase ownership and maintainability review](../queued/2026-07-18-codebase-ownership-and-maintainability-review.md).
That checkpoint starts from the reviewed merge on its own branch, runs the
requested multi-subagent antagonistic review, applies Checkpoint A-style
ownership/decomposition/maintainability refactors, and closes through its own
one-time gate and reviewed PR before Phase 7 begins. Checkpoint work must not be
mixed into this Phase 6 branch.

## Focused development verification

Run only the checks that cover each edited surface while developing:

- OpenAPI/examples and Python/Rust/TypeScript contract fixtures;
- Rust language-preference, migration, VAD/manifest, ledger-stage, result-
  admission, restart, cancellation, retention, and path-safety tests;
- React setup/settings/queue/history accessibility and state-projection tests;
- portable Python capability-catalog, component-lock, LID-probe, provider-route,
  result, alignment, containment, restart, corpus-exposure, exact-duration, and
  runtime-plan tests, plus the canonical explicit-profile word/grapheme,
  punctuation, and private-policy critical-token scorer;
- isolated public-fixture smoke/accuracy/resource checks on the relevant server
  component; and
- formatting, static analysis, dependency/provenance contracts, and diff checks
  for the changed languages only.

Do not run unrelated suites for a scoped change. Do not use a full Codex
Security plugin scan as a Phase 6 gate; normal security-aware design, focused
tests, dependency/provenance checks, and code review remain required. The next
full plugin scan is reserved for the accepted Phase 10 enterprise checkpoint,
and private scan material never enters this repository.

## One-time Phase 6 gate

The complete gate runs once only after code, contracts, docs, provenance,
focused reviews, and public-safe evidence are ready on one immutable candidate.
It must prove at least:

1. frontend install/build/unit/Playwright and required native WDIO pass;
2. Rust format, warnings-denied Clippy, library/integration/connector tests,
   dependency audit, and Windows dependency boundary pass;
3. portable Python 3.12 server/contract/component/runtime/infra tests pass;
4. backward-readable Phase 5 state plus Phase 6 migration/restart/cancel/retry/
   retention and result-admission behavior pass on the exact head;
5. the capability catalog exactly matches verified model locks and excludes
   Nemotron adaptation-ready locales;
6. primary/fixed/suggested/dynamic/unknown flows preserve explicit user choice,
   local offline switching emits monotonic source-bounded language spans without
   duplicate/dropped transcript text, and no path manufactures confidence;
7. VAD failure preserves source audio and bounded transport; alignment failure
   publishes an explicit unavailable result rather than fabricated timing;
8. the current advertised `en-US` route passes its bounded
   contract/capability smoke without publishing audio or transcripts, while
   completed local switching evidence remains honestly classified as Preview.
   No new Cohere natural-quality or long-duration campaign and no unmeasured
   locale/tier claim enters the gate. The completed AMI close/far run remains
   diagnostic only; provider-promotion subsets and independent multi-speaker,
   overlap, speaker-attributed, and virtual-meeting promotion belong to the
   later selected route and Phase 8 under ADR 0027;
9. GB10 uses the exact locked Python 3.12/NVIDIA runtime, records the bounded
   regression, streaming stability, exact-duration integrity, concurrency, and
   candidate resource ceilings, and leaves no owned container, process, model
   port, or listener. Neither resident service is promoted by this result; and
10. the LAN/SSH-forward development path still reconnects without claiming
    production authentication, persistent service, external networking, or an
    IT-owned enterprise deployment.

Any executable change after the complete run invalidates the candidate and
requires an explicit gate decision. Documentation-only evidence correction
must still be reviewed and must identify the unchanged checked SHA.

## Phase boundary and prohibited scope

- No Entra/MSAL, user/profile database, named speaker identity, OKF/agents/MCP,
  HTTP/3/UDP edge, external listener, certificate, DNS, ZPA, firewall policy,
  persistent production service, warm/multi-worker or mixed-load capacity
  promotion, production observability, or repo split.
- No adaptation-locale fine-tuning, unsupported language claim, automatic
  cross-provider voting, or token/word-level boundary claim beyond the exact
  source-time span granularity proved by the frozen gate.
- No new desktop ASR model router, bundled desktop Python runtime, second local
  lifecycle owner, or replacement of the pinned local Nemotron ASR contract.
- No private recordings, transcripts, scan findings, host paths, raw benchmark
  output, tokens, credentials, or enterprise configuration in Git or PRs.
- No ADR score/status inflation before implementation evidence exists.
